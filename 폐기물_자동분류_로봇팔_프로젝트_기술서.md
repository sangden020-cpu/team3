# 프로젝트 기술서

# RGB 카메라·YOLO·ArUco Homography 기반 폐기물 자동 분류 Pick & Place 시스템

---

## 1. 프로젝트 개요

### 1.1 프로젝트명

**RGB 카메라·YOLO·ArUco Homography 기반 폐기물 자동 분류 Pick & Place 시스템**

### 1.2 프로젝트 목적

본 프로젝트는 USB RGB 카메라로 작업판 위의 **캔(CAN), 페트병(PET), 종이(PAPER)** 를 인식하고, 영상에서 얻은 객체의 픽셀 좌표를 로봇 기준 XY 좌표로 변환한 뒤, Robotis OpenManipulator-X가 해당 물체를 집어 종류별 지정 위치로 분류하는 시스템을 구현하는 것을 목적으로 한다.

Depth Camera를 이용한 직접 거리 측정 방식 대신, 고정된 평면 작업판과 4개의 ArUco Marker를 이용하여 카메라 영상과 로봇 작업 좌표 사이의 Homography를 계산한다.

최종 시스템의 핵심 처리 과정은 다음과 같다.

```text
USB RGB Camera
      ↓
ROS 2 Image Topic
      ↓
YOLO 기반 PAPER / PET / CAN 인식
      ↓
Bounding Box Bottom Center Point 계산
      ↓
ArUco 4점 Homography
      ↓
Pixel → Robot XY 좌표 변환
      ↓
원점에서 가장 가까운 객체 선택
      ↓
거리별 좌표 오차 보정
      ↓
ROS 2 Topic 전달
      ↓
Arm / Gripper Action 제어
      ↓
OpenManipulator-X Pick & Place
      ↓
종류별 분류 위치로 이동
```

### 1.3 최종 구현 범위

- USB RGB Camera 영상 입력
- ROS 2 Camera Topic 발행
- PAPER / PET / CAN 객체 인식
- YOLO 검출 결과 Topic 발행
- ArUco Marker 4개 기반 작업 좌표 보정
- Homography 기반 Pixel → Robot XY 변환
- 객체별 거리 계산 및 가장 가까운 객체 선택
- 가까운 영역 / 먼 영역에 서로 다른 좌표 가중치 적용
- 물체 종류별 Pick 높이 적용
- 물체 종류별 Place 위치 적용
- 공중 안전 자세를 이용한 Pick & Place
- ROS 2 ActionClient 기반 Arm / Gripper 제어
- RQt Graph를 이용한 Node / Topic / Action 확인
- TF `view_frames` 확인
- Flask 기반 Web Dashboard 구성
- 시연 영상 촬영

> 현재 로봇 제어는 MoveIt 2 Planning을 최종 제어 방식으로 사용하지 않고, `FollowJointTrajectory`와 `GripperCommand` Action을 직접 호출하는 방식으로 구현하였다.

---

# 2. 시스템 전체 구성

## 2.1 전체 시스템 구성도

```text
                         USB RGB Camera
                                │
                                ▼
                  aruco_homography_calibration
                                │
                 /detect_trash/image_raw
                                │
                                ▼
                         waste_detector
                                │
                  /detect_trash/detections
                                │
                                ▼
                  aruco_homography_calibration
                                │
              ┌─────────────────┴─────────────────┐
              │                                   │
              ▼                                   ▼
       Pixel → Robot XY                    객체별 거리 계산
                                                  │
                                                  ▼
                                        가장 가까운 객체 선택
                                                  │
                                                  ▼
                              /detect_trash/nearest_target
                                                  │
                                                  ▼
                                          waste_pick_node
                                      ┌───────────┴───────────┐
                                      ▼                       ▼
                         FollowJointTrajectory          GripperCommand
                                      │                       │
                                      ▼                       ▼
                              arm_controller          gripper_controller
                                      └───────────┬───────────┘
                                                  ▼
                                       OpenManipulator-X
                                                  │
                                                  ▼
                                        Pick & Place 수행
```

## 2.2 Dashboard 구성

```text
/detect_trash/image_raw ───────┐
/detect_trash/detections ──────┤
/detect_trash/nearest_target ──┼──> waste_dashboard
/joint_states ─────────────────┘          │
                                         ▼
                                       Flask
                                         │
                                         ▼
                               http://127.0.0.1:5000
```

Dashboard에서는 다음 정보를 확인할 수 있다.

- 실시간 카메라 영상
- YOLO Bounding Box
- PAPER / PET / CAN 검출 개수
- 현재 가장 가까운 Target
- Target Robot X / Y 좌표
- Target 거리
- Confidence
- ROS 2 Topic 상태
- Robot Joint State
- 현재 Detection 목록

---

# 3. 하드웨어 구성

## 3.1 USB RGB Camera

본 시스템에서는 작업판 전체를 촬영하기 위해 USB RGB Camera를 사용한다.

하나의 RGB 영상으로 다음 두 가지 Vision 처리를 동시에 수행한다.

1. ArUco Marker 검출
2. PAPER / PET / CAN 객체 검출

본 프로젝트에서는 최종적으로 Depth Camera의 Depth 값을 사용하지 않는다.

작업판의 높이가 고정된 평면이라는 조건을 활용하여, X/Y 위치는 Homography로 계산하고 Z축은 객체 종류 및 실험값에 따라 고정값을 사용한다.

카메라 영상은 ROS 2 Topic으로 전달되어 객체 인식 노드에서 사용된다.

```text
Topic : /detect_trash/image_raw
Type  : sensor_msgs/Image
```

---

## 3.2 작업판

폐기물이 놓이는 평면 작업 공간이다.

최종 좌표계에서는 작업판 한 모서리를 단순 원점으로 사용하는 것이 아니라, **OpenManipulator-X의 `link1` 위치를 Robot Origin `(0,0)` 기준으로 사용한다.**

작업 영역의 좌표는 다음과 같이 설정하였다.

```text
X : 0.0 m ~ 0.4 m
Y : -0.2 m ~ 0.2 m
```

ArUco Marker의 대응 좌표는 다음과 같다.

```text
ID 1 = (0.4,  0.2)
ID 2 = (0.4, -0.2)
ID 3 = (0.0, -0.2)
ID 4 = (0.0,  0.2)
```

개념적으로 다음과 같은 좌표 영역을 구성한다.

```text
Y +0.2 m

ID 4 ●────────────────────────────● ID 1
     │                            │
     │          작업 영역          │
     │                            │
     │                            │
ID 3 ●────────────────────────────● ID 2

Y -0.2 m

X = 0.0 m                     X = 0.4 m
↑
link1 기준 Origin 방향
```

---

## 3.3 ArUco Marker

ArUco Marker ID 1~4를 작업 영역의 기준점으로 사용한다.

ArUco Marker의 목적은 폐기물 종류를 판별하는 것이 아니라 다음과 같다.

- 작업판의 기준점 검출
- Camera Pixel 좌표와 Robot XY 좌표의 대응점 제공
- Homography Matrix 계산
- 작업 영역 좌표계 생성

4개 기준점이 모두 검출되어 Homography가 계산되면 해당 값을 LOCK한다.

이를 통해 순간적인 Marker 미검출 또는 Center Point 흔들림으로 Homography가 계속 변경되는 문제를 방지한다.

필요한 경우 Calibration Reset 후 다시 4개 기준점을 확보할 수 있다.

---

## 3.4 Robotis OpenManipulator-X

OpenManipulator-X는 Vision 시스템에서 계산된 폐기물 위치로 이동하여 Pick & Place를 수행한다.

최종 제어 방식은 MoveIt 2 경로 계획이 아니라 ROS 2 ActionClient를 이용한 직접 관절 제어이다.

Arm 제어:

```text
Action:
/arm_controller/follow_joint_trajectory

Type:
control_msgs/action/FollowJointTrajectory
```

Gripper 제어:

```text
Action:
/gripper_controller/gripper_cmd

Type:
control_msgs/action/GripperCommand
```

---

## 3.5 Gripper

OpenManipulator-X 말단의 Gripper는 객체 파지 및 배치에 사용한다.

동작은 다음 두 값으로 구분한다.

```text
GRIPPER_OPEN
GRIPPER_HOLD
```

Pick 위치까지 하강한 후 `GRIPPER_HOLD` 위치로 닫고, 분류 위치로 이동한 후 다시 `GRIPPER_OPEN`으로 개방한다.

---

# 4. 소프트웨어 및 개발 환경

| 구분 | 사용 환경 / 기술 |
|---|---|
| Host OS | Windows |
| Linux 환경 | WSL2 |
| Linux 배포판 | Ubuntu 24.04 |
| Robot Middleware | ROS 2 Jazzy |
| Programming Language | Python |
| Image Processing | OpenCV |
| Object Detection | Roboflow Inference 기반 YOLO 객체 검출 |
| Marker Detection | OpenCV ArUco |
| Coordinate Transformation | Homography |
| Robot Control | ros2_control + ROS 2 ActionClient |
| Robot | Robotis OpenManipulator-X |
| Camera | USB RGB Camera |
| Dashboard | Flask |
| ROS Visualization | RQt Graph, TF2 Tools |
| AI Python 환경 | 별도 Python virtual environment |

---

# 5. ROS 2 패키지 구성

주요 프로젝트 패키지는 `detect_trash`이다.

```text
khj_ws/src/detect_trash/
├── detect_trash/
│   ├── __init__.py
│   ├── aruco_homography_calibration.py
│   ├── detector_node.py
│   ├── robot_control.py
│   └── dashboard_node.py
├── resource/
│   └── detect_trash
├── test/
├── package.xml
├── setup.cfg
└── setup.py
```

주요 파일 역할은 다음과 같다.

| 파일 | 역할 |
|---|---|
| `aruco_homography_calibration.py` | Camera, ArUco, Homography, Robot XY 변환, 최근접 객체 선택 |
| `detector_node.py` | PAPER / PET / CAN 객체 인식 및 Detection Topic 발행 |
| `robot_control.py` | 가장 가까운 객체 수신 및 Pick & Place |
| `dashboard_node.py` | ROS 2 데이터를 Flask 웹 Dashboard로 표시 |

---

# 6. AI 객체 인식 시스템

## 6.1 객체 인식 대상

최종 프로젝트에서는 다음 3종류의 폐기물을 대상으로 한다.

- PAPER
- PET
- CAN

초기 테스트에서 사용했던 재활용 가능 / 불가능 분류 모델은 최종 Pick & Place 시스템에서는 사용하지 않는다.

---

## 6.2 사용 모델

| 객체 | Model ID | Confidence Threshold |
|---|---|---:|
| PAPER | `crumpledpaper/1` | 0.70 |
| PET | `plastic-bottles-ip5yb-uziag-hg1ll/1` | 0.85 |
| CAN | `can-a8pgu/2` | 0.75 |

각 모델의 결과는 공통 형식으로 정리하여 ROS 2 Topic으로 발행한다.

---

## 6.3 모델 병렬 처리

PAPER, PET, CAN 3개의 모델을 순차 처리하면 전체 Detection 갱신 속도가 느려질 수 있다.

따라서 `ThreadPoolExecutor`를 사용하여 모델별 추론을 비동기적으로 처리한다.

개념적인 구조는 다음과 같다.

```text
                 Latest Camera Frame
                        │
          ┌─────────────┼─────────────┐
          ▼             ▼             ▼
      PAPER Model    PET Model     CAN Model
          │             │             │
          └─────────────┼─────────────┘
                        ▼
                  Detection Merge
                        │
                        ▼
             /detect_trash/detections
```

---

# 7. 폐기물 객체 인식 과정

카메라 영상이 입력되면 YOLO 기반 모델을 이용하여 폐기물을 검출한다.

검출 결과에는 다음 정보가 포함된다.

```text
type
class
confidence
x1
y1
x2
y2
center_x
center_y
```

예시:

```json
{
  "type": "can",
  "class": "can",
  "confidence": 0.87,
  "x1": 400,
  "y1": 180,
  "x2": 480,
  "y2": 320,
  "center_x": 440,
  "center_y": 320
}
```

이 결과는 JSON 문자열 형태로 다음 Topic에 전달된다.

```text
Topic : /detect_trash/detections
Type  : std_msgs/String
```

---

# 8. Bottom Center Point 기반 Pick 기준점

## 8.1 Bottom Center Point 사용 이유

Bounding Box의 단순 중심점을 사용하면 세워진 캔이나 PET병의 경우 객체 높이의 중간 지점을 가리키게 된다.

본 프로젝트에서는 작업판 평면의 실제 객체 위치와 더 가까운 점을 얻기 위해 Bounding Box의 **하단 중앙점**을 객체의 기준 Pixel로 사용한다.

Bounding Box 좌표가 `(x1, y1)`, `(x2, y2)`라면 다음과 같이 계산한다.

```text
center_x = (x1 + x2) / 2
center_y = y2
```

따라서:

```text
Pick Pixel = (center_x, center_y)
```

이다.

---

## 8.2 의미

Bottom Center Point는 실제 물리적 중심을 직접 측정한 값이 아니라, 객체가 작업판과 접촉하는 위치에 가까운 Pixel을 얻기 위한 영상 기반 기준점이다.

```text
YOLO Bounding Box
       ↓
Bounding Box 하단
       ↓
하단 중앙점 계산
       ↓
Object Reference Pixel
       ↓
Homography
       ↓
Robot XY
```

---

# 9. ArUco 기반 Robot XY 좌표계

## 9.1 Robot Origin

최종 시스템에서는 `link1`을 기준으로 Robot XY 좌표계를 구성한다.

```text
Origin = link1 = (0, 0)
```

객체 위치도 최종적으로 `link1` 기준 좌표로 사용한다.

`PointStamped` 메시지를 발행할 때 Frame은 다음과 같이 지정한다.

```text
frame_id = link1
```

---

## 9.2 Marker 대응점

ArUco Marker ID별 Robot Coordinate는 다음과 같다.

```text
ID 1 → (0.4,  0.2)
ID 2 → (0.4, -0.2)
ID 3 → (0.0, -0.2)
ID 4 → (0.0,  0.2)
```

Camera Image에서 검출된 Marker Center Pixel 4개와 위 Robot Coordinate 4개를 대응시켜 Homography를 계산한다.

---

# 10. Homography 기반 좌표 변환

## 10.1 필요성

YOLO의 Bottom Center Point는 Camera Image의 Pixel 좌표이므로 로봇이 직접 사용할 수 없다.

OpenManipulator-X가 사용할 수 있도록 Pixel `(u,v)`를 `link1` 기준 Robot Coordinate `(X,Y)`로 변환해야 한다.

```text
Camera Pixel
   (u, v)
      │
      ▼
 Homography
      │
      ▼
Robot XY
   (X, Y)
```

---

## 10.2 Homography Matrix

4개의 대응점을 이용하여 Homography Matrix `H`를 계산한다.

```python
H = cv2.getPerspectiveTransform(
    image_points,
    robot_points
)
```

개념적으로:

```text
      [u]
      [v]
      [1]
       │
       │ H
       ▼
      [X']
      [Y']
      [W']
```

최종 좌표는 다음과 같이 정규화된다.

```text
X = X' / W'
Y = Y' / W'
```

실제 코드에서는 OpenCV의 `perspectiveTransform()`을 이용한다.

```python
robot_point = cv2.perspectiveTransform(
    pixel_point,
    H
)
```

---

# 11. ArUco Calibration LOCK

실시간으로 Marker를 계속 검출하면서 Homography를 매 프레임 다시 계산하면 Marker Center의 작은 흔들림이 Robot XY 좌표 변화로 이어질 수 있다.

이를 줄이기 위해 다음 방식으로 동작한다.

```text
ID 1~4 검출
      ↓
4개 기준점 확보
      ↓
Homography 계산
      ↓
Calibration LOCK
      ↓
고정된 H 사용
```

필요한 경우 Reset 기능을 이용해 새로운 Homography를 다시 계산한다.

---

# 12. 폐기물 좌표 계산 과정

전체 좌표 계산은 다음 순서로 수행한다.

```text
Camera Frame
    │
    ├──────────────→ ArUco ID 1~4
    │                    │
    │                    ▼
    │             Homography Matrix H
    │
    └──────────────→ YOLO Detection
                         │
                         ▼
                Bottom Center Pixel
                         │
                         ▼
              cv2.perspectiveTransform
                         │
                         ▼
                  Robot X / Y
                         │
                         ▼
                  Distance 계산
```

---

# 13. 가장 가까운 객체 우선 선택

## 13.1 거리 계산

동시에 여러 객체가 검출될 경우 `link1` Origin에서 각 객체까지의 거리를 계산한다.

```text
distance = sqrt(X² + Y²)
```

예:

```text
PAPER : 0.27 m
PET   : 0.22 m
CAN   : 0.34 m
```

거리 기준으로 오름차순 정렬하면:

```text
1. PET
2. PAPER
3. CAN
```

이 되고, PET를 다음 Pick Target으로 선택한다.

---

## 13.2 최근접 Target Topic

가장 가까운 객체의 정보는 다음 Topic으로 발행한다.

```text
Topic : /detect_trash/nearest_target
Type  : std_msgs/String
```

전달 정보 예시는 다음과 같다.

```json
{
  "type": "pet",
  "class": "plastic-bottle",
  "confidence": 0.92,
  "pixel_x": 144,
  "pixel_y": 263,
  "x": 0.2056,
  "y": -0.1781,
  "distance": 0.272
}
```

Robot Control Node는 이 메시지를 받아 현재 Pick 대상을 결정한다.

---

# 14. 거리별 Pick 좌표 오차 보정

## 14.1 보정 필요성

실제 OpenManipulator-X에서 근사 IK를 사용해 Pick 위치를 테스트한 결과, 물체가 Robot Origin에서 가까운 경우와 먼 경우에 서로 다른 방향의 위치 오차가 나타났다.

실험 결과 `(X=0.30 m, Y=0.07 m)` 부근에서는 추가 보정 없이 비교적 정확하게 Pick할 수 있었다.

따라서 해당 위치의 원점 거리를 보정 기준으로 사용하였다.

```text
REFERENCE_DISTANCE
= sqrt(0.30² + 0.07²)
≈ 0.308 m
```

---

## 14.2 가까운 영역과 먼 영역 분리

기준 거리보다 가까운 경우 Robot Arm이 실제 Target보다 멀리 도달하는 경향이 있어 음수 가중치를 사용한다.

기준보다 먼 경우에는 Robot Arm이 Target까지 충분히 도달하지 못하는 경향이 있어 양수 가중치를 사용한다.

```text
Near Target
     ↓
Negative Weight
     ↓
X/Y 좌표 축소

Reference Distance
     ↓
0 % Correction

Far Target
     ↓
Positive Weight
     ↓
X/Y 좌표 확대
```

가까운 영역과 먼 영역의 오차 특성이 다르기 때문에 Gain을 별도로 설정한다.

예시 설정:

```text
NEAR_GAIN = 3.0
FAR_GAIN  = 1.5

MIN_WEIGHT = -0.30
MAX_WEIGHT = +0.15
```

이 방식은 정밀한 기구학적 보정보다는 실제 작업판에서 얻은 실험값을 이용한 경험적 보정이다.

---

# 15. Z축 위치 처리

본 시스템에서는 Depth Camera 값을 사용하지 않으므로 객체의 Z 위치를 Camera로 직접 계산하지 않는다.

대신:

- 작업판 높이 고정
- 객체 종류별 Pick 높이 고정
- 안전 이동 높이 고정

조건을 사용한다.

개념적인 Pick Z 동작:

```text
        Safe / Approach Z
              │
              ▼
        Object Above Position
              │
              ▼
            Pick Z
              │
              ▼
         Gripper Close
              │
              ▼
             Lift
```

현재 프로젝트에서는 물체 특성에 따라 Pick 높이를 개별 설정할 수 있도록 구성하였다.

---

# 16. ROS 2 Node 구성

## 16.1 주요 Node

| Node | 역할 |
|---|---|
| `/aruco_homography_calibration` | Camera 입력, ArUco, Homography, Robot XY, 최근접 Target |
| `/waste_detector` | PAPER / PET / CAN 객체 검출 |
| `/waste_pick_node` | Target 수신 및 Robot Pick & Place |
| `/waste_dashboard` | ROS 2 데이터를 Flask Dashboard로 전달 |
| `/robot_state_publisher` | Robot TF 발행 |
| `/joint_state_broadcaster` | Joint State 발행 |
| `/arm_controller` | Arm Trajectory 제어 |
| `/gripper_controller` | Gripper 제어 |
| `/controller_manager` | ros2_control Controller 관리 |

---

# 17. ROS 2 Topic 구성

| Topic | Type | 주요 Publisher | 주요 Subscriber | 용도 |
|---|---|---|---|---|
| `/detect_trash/image_raw` | `sensor_msgs/Image` | aruco_homography_calibration | waste_detector, waste_dashboard | Camera 영상 |
| `/detect_trash/detections` | `std_msgs/String` | waste_detector | aruco_homography_calibration, waste_dashboard | YOLO 결과 |
| `/detect_trash/target_point` | `geometry_msgs/PointStamped` | aruco_homography_calibration | 필요 시 사용 | 최근접 객체 좌표 |
| `/detect_trash/nearest_target` | `std_msgs/String` | aruco_homography_calibration | waste_pick_node, waste_dashboard | 최근접 객체 전체 정보 |
| `/joint_states` | `sensor_msgs/JointState` | joint_state_broadcaster | robot_state_publisher, waste_dashboard | Robot Joint 상태 |
| `/tf` | `tf2_msgs/TFMessage` | robot_state_publisher 등 | TF Client | Frame Transform |

---

# 18. ROS 2 Action 구성

최종 Robot Control은 MoveIt 2 Planning 대신 ActionClient를 사용한다.

## 18.1 Arm Action

```text
/arm_controller/follow_joint_trajectory
```

Type:

```text
control_msgs/action/FollowJointTrajectory
```

`joint1`, `joint2`, `joint3`, `joint4`의 목표 각도를 하나의 Trajectory Point로 전달한다.

---

## 18.2 Gripper Action

```text
/gripper_controller/gripper_cmd
```

Type:

```text
control_msgs/action/GripperCommand
```

Gripper Open / Hold 명령에 사용한다.

---

# 19. Robot Joint Angle 계산

## 19.1 Joint 1

Target XY 방향은 다음과 같이 계산한다.

```text
joint1 = atan2(Y, X)
```

이를 통해 Robot Base가 객체 방향으로 회전한다.

---

## 19.2 Joint 2~4

본 프로젝트의 최종 제어에서는 범용 IK Solver 대신 고정 작업 공간에 맞춘 실험 기반 근사 IK를 사용한다.

개념적으로:

```text
r = sqrt(X² + Y²)

joint2 = Z 차이 기반 근사
joint3 = 거리 r 및 Z 차이 기반 근사
joint4 = -(joint2 + joint3)
```

`joint4`는 Gripper 방향을 일정하게 유지하기 위한 보정에 사용한다.

본 방식은 특정 작업판 범위에 최적화된 방식이며, 모든 Cartesian Position에 대해 정확한 해를 보장하는 범용 IK는 아니다.

---

# 20. 공중 안전 이동 기반 Pick & Place

로봇이 작업판과 가까운 높이에서 Target 사이를 직접 이동하면 다른 물체 또는 작업판과 충돌할 위험이 있다.

따라서 Pick 및 Place 시 **공중 안전 자세**를 이용한다.

Pick 단계:

```text
HOME
 ↓
Gripper Open
 ↓
Safe Pose 상승
 ↓
공중에서 Target 방향으로 회전
 ↓
Target 상공으로 이동
 ↓
Pick Z까지 하강
 ↓
Gripper Close
 ↓
Lift
```

Place 단계:

```text
Lift
 ↓
Safe Pose 상승
 ↓
공중에서 Bin 방향으로 회전
 ↓
Bin 위치로 이동
 ↓
Gripper Open
 ↓
Safe Pose 상승
 ↓
정면 방향 복귀
 ↓
HOME
```

공중 이동용 Joint Pose는 고정된 안전 자세를 이용한다.

예시:

```text
Joint2 Safe = -35 deg
Joint3 Safe = -30 deg
Joint4 Safe = -(Joint2 + Joint3)
```

---

# 21. 객체별 자동 분류

YOLO가 반환한 `type`을 이용하여 Place 위치를 선택한다.

```text
PAPER → Paper Bin
PET   → PET Bin
CAN   → Can Bin
```

Robot Control Node는 Target의 `type` 값을 확인하고 객체별로 미리 정의된 Place Pose를 선택한다.

개념:

```python
BIN_POSES = {
    "can":   (...),
    "pet":   (...),
    "paper": (...)
}
```

따라서 Vision에서 얻은 **객체 이름 자체를 Robot 행동 결정에 사용한다.**

---

# 22. 전체 Pick & Place 알고리즘

```text
① USB RGB Camera 영상 획득
        ↓
② /detect_trash/image_raw 발행
        ↓
③ ArUco ID 1~4 검출
        ↓
④ Homography 계산 및 LOCK
        ↓
⑤ PAPER / PET / CAN YOLO 검출
        ↓
⑥ Bounding Box 생성
        ↓
⑦ Bottom Center Point 계산
        ↓
⑧ /detect_trash/detections 발행
        ↓
⑨ Pixel → Robot XY 변환
        ↓
⑩ 객체별 Origin 거리 계산
        ↓
⑪ 가장 가까운 객체 선택
        ↓
⑫ /detect_trash/nearest_target 발행
        ↓
⑬ Robot Control에서 Target 수신
        ↓
⑭ Workspace / Timeout 검사
        ↓
⑮ 거리별 XY 가중치 보정
        ↓
⑯ Gripper Open
        ↓
⑰ 안전 고도로 상승
        ↓
⑱ Target 방향으로 공중 이동
        ↓
⑲ Target 상공에서 Pick Z로 하강
        ↓
⑳ Gripper Close
        ↓
㉑ Lift
        ↓
㉒ 객체 종류별 Bin 선택
        ↓
㉓ 공중 안전 자세로 Bin 방향 이동
        ↓
㉔ Gripper Open
        ↓
㉕ Home 복귀
```

현재 안전한 테스트 및 시연을 위해 사용자가 실행 입력을 주면 **현재 가장 가까운 객체 1개를 선택하여 한 번의 Pick & Place Sequence를 수행**하는 구조로 사용한다.

---

# 23. 상태 머신

프로젝트 동작을 상태 머신 관점으로 표현하면 다음과 같다.

```text
IDLE
 ↓
OBJECT_DETECT
 ↓
TARGET_SELECT
 ↓
MOVE_SAFE
 ↓
MOVE_ABOVE_TARGET
 ↓
DESCEND
 ↓
GRASP
 ↓
LIFT
 ↓
MOVE_TO_BIN
 ↓
RELEASE
 ↓
RETURN_HOME
 ↓
IDLE
```

각 상태의 의미는 다음과 같다.

| 상태 | 의미 |
|---|---|
| `IDLE` | Robot 대기 |
| `OBJECT_DETECT` | PAPER / PET / CAN 인식 |
| `TARGET_SELECT` | 가장 가까운 객체 선택 |
| `MOVE_SAFE` | 공중 안전 자세로 이동 |
| `MOVE_ABOVE_TARGET` | 객체 상공으로 이동 |
| `DESCEND` | Pick 높이까지 하강 |
| `GRASP` | Gripper를 닫아 객체 파지 |
| `LIFT` | 객체를 든 상태로 상승 |
| `MOVE_TO_BIN` | 객체 종류별 분류 위치로 이동 |
| `RELEASE` | Gripper Open |
| `RETURN_HOME` | 초기 자세로 복귀 |
| `ERROR` | 오류 발생 시 다음 단계 진행 중단 |

---

# 24. 오류 처리

| 오류 상황 | 감지 방법 | 처리 방법 |
|---|---|---|
| 객체가 인식되지 않음 | Detection 결과 없음 | Robot 이동하지 않고 대기 |
| Homography 보정 전 | `H is None` | 좌표 변환 및 Target 발행 중단 |
| Detection JSON 오류 | JSON Decode 예외 | 해당 메시지 무시 |
| 오래된 Target | Target Timeout 초과 | 기존 좌표 무시 후 재인식 |
| 작업 영역 밖 객체 | X/Y 범위 검사 | 해당 Target 제외 |
| 알 수 없는 객체 종류 | `type` 검사 | Pick 대상에서 제외 |
| 분류 위치 미설정 | Bin Pose 검사 | Sequence 중단 및 오류 로그 |
| Arm Action Server 연결 실패 | `wait_for_server()` 실패 | Arm 이동 중단 |
| Arm Trajectory 실패 | Action Result 검사 | 다음 단계 진행하지 않음 |
| Gripper Action Server 연결 실패 | `wait_for_server()` 실패 | Sequence 중단 |
| Gripper Action 실패 | Action Result 검사 | Sequence 중단 |
| Camera Open 실패 | VideoCapture 상태 확인 | Camera Node 중단 |
| Camera Frame 수신 실패 | Frame Read 실패 | Vision 처리 중단 |
| ArUco Marker 4개 미검출 | Marker ID 검사 | Calibration 완료 전 좌표 변환 중단 |
| Calibration 이후 Marker 순간 미검출 | LOCK 상태 | 기존 Homography 유지 |
| Robot 도달 범위 밖 Target | Workspace / 거리 검사 | 대상 제외 또는 보정 |

---

# 25. TF 구성 및 확인

## 25.1 TF 확인 방법

TF Frame 관계는 다음 명령으로 확인한다.

```bash
ros2 run tf2_tools view_frames
```

특정 두 Frame의 Transform은 다음 명령으로 확인할 수 있다.

```bash
ros2 run tf2_ros tf2_echo link1 link5
```

---

## 25.2 실제 확인 결과

`view_frames` 실행 당시 확인된 주요 Frame 관계는 다음과 같다.

```text
world
 └── link1

link5
 ├── camera_link
 └── end_effector_link
```

| Parent Frame | Child Frame | 의미 |
|---|---|---|
| `world` | `link1` | Robot 기준 시작 Frame |
| `link5` | `camera_link` | Camera 관련 Frame |
| `link5` | `end_effector_link` | End Effector Frame |

해당 실행 시점의 `view_frames`에서는 `link1`부터 `link5`까지의 전체 동적 Robot Link Chain이 하나의 Tree로 모두 표시되지는 않았다.

따라서 TF 자료는 실제 실행 시점의 Frame 구성 확인 자료로 사용하며, 전체 Robot Link TF 연결 상태는 Controller 및 `joint_states`와 함께 추가 검증 대상으로 둔다.

> 본 프로젝트의 Vision 좌표 변환 자체는 ArUco Homography를 통해 계산된 Robot XY를 `link1` 기준으로 사용한다.

---

# 26. Web Dashboard

## 26.1 목적

공통 필수 기능인 Dashboard를 구현하기 위해 Flask 기반 웹 화면을 구성하였다.

Dashboard Node는 ROS 2 Topic을 구독하고, 수신한 데이터를 웹 브라우저에서 볼 수 있도록 전달하는 Bridge 역할을 한다.

---

## 26.2 구독 Topic

```text
/detect_trash/image_raw
/detect_trash/detections
/detect_trash/nearest_target
/joint_states
```

---

## 26.3 표시 정보

- 실시간 영상
- Detection Bounding Box
- PAPER / PET / CAN 개수
- 최근접 Target
- Target Class
- Confidence
- Robot X / Y
- Target Distance
- Pixel Coordinate
- Topic Online / Offline 상태
- Joint State
- Detection List

---

## 26.4 실행

```bash
ros2 run detect_trash dashboard_node
```

브라우저:

```text
http://127.0.0.1:5000
```

Flask의 개발 서버 경고 메시지는 시연용 로컬 서버 실행에서 발생하는 일반적인 메시지이며, 프로젝트 시연 환경에서는 로컬 Dashboard 확인 용도로 사용한다.

---

# 27. RQt Graph를 이용한 ROS 2 통신 확인

다음 명령으로 ROS Graph를 확인한다.

```bash
rqt_graph
```

프로젝트에서 확인할 수 있는 주요 연결은 다음과 같다.

```text
aruco_homography_calibration
       ↓
/detect_trash/image_raw
       ↓
waste_detector
       ↓
/detect_trash/detections
       ↓
aruco_homography_calibration
       ↓
/detect_trash/nearest_target
       ↓
waste_pick_node
       ↓
Arm / Gripper Action
```

Dashboard를 실행한 상태에서는 `/waste_dashboard`가 다음 Topic을 구독하는 모습도 RQt Graph에서 확인할 수 있다.

---

# 28. 실행 절차

프로젝트는 여러 Terminal에서 각 Node를 순서대로 실행한다.

## Terminal 1 - OpenManipulator-X Hardware

```bash
source /opt/ros/jazzy/setup.bash
source ~/2026_manipulator/open_manipulator_ws/install/setup.bash
source ~/2026_manipulator/khj_ws/install/setup.bash

ros2 launch open_manipulator_bringup \
  open_manipulator_x.launch.py
```

---

## Terminal 2 - Camera / ArUco / Homography

```bash
source /opt/ros/jazzy/setup.bash
source ~/2026_manipulator/khj_ws/install/setup.bash

ros2 run detect_trash \
  aruco_homography_calibration
```

ArUco ID 1~4가 검출되면 Homography가 계산되고 LOCK된다.

---

## Terminal 3 - YOLO Detector

Roboflow API Key가 환경 변수로 설정되어 있어야 한다.

```bash
export ROBOFLOW_API_KEY="YOUR_API_KEY"
```

AI 가상환경의 Python으로 실행한다.

```bash
/home/khjoo/2026_manipulator/perception_envs/depth_yolo/.venv/bin/python \
~/2026_manipulator/khj_ws/src/detect_trash/detect_trash/detector_node.py
```

---

## Terminal 4 - Robot Control

```bash
source /opt/ros/jazzy/setup.bash
source ~/2026_manipulator/khj_ws/install/setup.bash

ros2 run detect_trash robot_control
```

사용자 입력:

```text
Enter : 현재 가장 가까운 객체 1개 Pick & Place
q     : 종료
```

---

## Terminal 5 - Dashboard

```bash
source /opt/ros/jazzy/setup.bash
source ~/2026_manipulator/khj_ws/install/setup.bash

ros2 run detect_trash dashboard_node
```

브라우저:

```text
http://127.0.0.1:5000
```

---

## Terminal 6 - ROS Graph

```bash
rqt_graph
```

---

# 29. 결과 확인 방법

## 29.1 OpenCV Window

카메라 영상에서 다음 정보를 확인할 수 있다.

- PAPER / PET / CAN Bounding Box
- Confidence
- Bottom Center Point
- ArUco Marker
- Calibration LOCK
- Robot Origin
- Robot XY 출력

---

## 29.2 Topic 확인

Detection:

```bash
ros2 topic echo /detect_trash/detections --once
```

Nearest Target:

```bash
ros2 topic echo /detect_trash/nearest_target --once
```

Camera Topic Hz:

```bash
ros2 topic hz /detect_trash/image_raw
```

Joint State:

```bash
ros2 topic echo /joint_states --once
```

---

## 29.3 Node / Topic / Action 확인

```bash
rqt_graph
```

---

## 29.4 TF 확인

```bash
ros2 run tf2_tools view_frames
```

---

## 29.5 Dashboard 확인

```text
http://127.0.0.1:5000
```

---

# 30. 공통 필수 기능 충족 내용

| 공통 필수 기능 | 프로젝트 구현 내용 |
|---|---|
| ROS 2 Camera Topic 또는 저장 영상 입력 | `/detect_trash/image_raw` 사용 |
| Vision 처리 | YOLO + ArUco + Homography |
| Detection 결과 전달/기록 | `/detect_trash/detections`, `/detect_trash/nearest_target` |
| Vision 결과와 Manipulator 동작 연결 | 최근접 객체 좌표 → OpenManipulator-X Pick & Place |
| Dashboard 구성 | Flask Web Dashboard |
| 주요 Node 실행 절차 | 다중 Terminal 실행 절차 작성 |
| 결과 확인 | OpenCV Window, RQt, TF, Browser Dashboard, 시연 영상 |

---

# 31. 최종 구현 결과

최종적으로 다음 기능을 구현하였다.

- [x] USB RGB Camera 입력
- [x] ROS 2 Image Topic
- [x] PAPER 객체 인식
- [x] PET 객체 인식
- [x] CAN 객체 인식
- [x] YOLO 결과 Topic 발행
- [x] Bottom Center Point 추출
- [x] ArUco Marker 4점 검출
- [x] Homography Calibration
- [x] Calibration LOCK
- [x] Pixel → Robot XY 변환
- [x] 가장 가까운 객체 선택
- [x] 객체 종류 문자열을 Robot 행동 결정에 활용
- [x] 거리별 XY 좌표 보정
- [x] ROS 2 Action 기반 Arm 제어
- [x] ROS 2 Action 기반 Gripper 제어
- [x] 공중 안전 이동
- [x] 종류별 분류 위치
- [x] RQt Graph 확인
- [x] TF `view_frames` 확인
- [x] Flask Dashboard
- [x] 시연 영상

---

# 32. 시스템의 핵심 기술

## 32.1 RGB Camera 기반 Vision

Depth 정보를 사용하지 않고 RGB Camera 하나에서 ArUco와 객체 인식을 동시에 수행한다.

---

## 32.2 ArUco Homography

ArUco Marker ID 1~4를 Robot XY 대응점으로 사용하여 Camera Pixel을 `link1` 기준 XY로 변환한다.

---

## 32.3 Bottom Center Point

Bounding Box 중심 대신 하단 중앙점을 사용해 작업판과 접촉하는 위치에 가까운 Pixel을 얻는다.

---

## 32.4 최근접 객체 선택

동시에 여러 객체가 존재해도 모든 Robot XY를 계산한 후 Origin에서 가장 가까운 객체를 먼저 선택한다.

---

## 32.5 경험적 거리 보정

기준 거리 약 30.8 cm를 중심으로 가까운 Target에는 음수, 먼 Target에는 양수 가중치를 적용한다.

---

## 32.6 ROS 2 Topic + Action

Vision Node 간에는 Topic으로 데이터를 전달하고, 실제 Robot Motion은 Action을 이용한다.

```text
Vision Data
   ↓
ROS 2 Topic

Robot Motion
   ↓
ROS 2 Action
```

---

## 32.7 Safe Flight Motion

물체를 집으러 가거나 Bin으로 이동할 때 작업판 가까이에서 횡이동하지 않고 먼저 안전 자세로 상승한 뒤 공중에서 방향을 변경한다.

---

## 32.8 Web Dashboard

ROS 2 데이터를 Flask를 통해 웹으로 표시함으로써 Vision 결과와 Robot 상태를 브라우저에서 확인할 수 있도록 하였다.

---

# 33. 한계점

## 33.1 Depth 정보 미사용

Homography는 평면상의 XY 좌표 변환에 적합하다.

따라서 작업판 높이가 변경되거나 객체의 3차원 위치를 직접 구해야 하는 환경에서는 한계가 있다.

---

## 33.2 근사 IK

현재 Robot XY → Joint Angle 변환은 실험 기반 근사식이다.

따라서 작업 공간 전체에서 동일한 위치 정확도를 보장하지 않는다.

---

## 33.3 경험적 거리 가중치

현재 좌표 보정은 실제 실험을 통해 얻은 기준 위치 및 Gain을 사용한다.

환경이나 Camera 위치가 변경되면 재조정이 필요할 수 있다.

---

## 33.4 Robot Reach

OpenManipulator-X의 실제 작업 가능 영역보다 먼 객체는 좌표를 계산할 수 있어도 Robot이 물리적으로 도달할 수 없다.

따라서 Cartesian Workspace뿐 아니라 실제 Robot Reach를 고려한 작업 영역 설정이 필요하다.

---

## 33.5 TF Tree

실행 시점의 `view_frames`에서 Robot Link 전체가 하나의 Tree로 모두 나타나지 않은 상태를 확인하였다.

향후 Controller / Joint State / Robot State Publisher 관계를 추가 점검하여 전체 Link Chain의 TF를 안정적으로 구성할 수 있다.

---

## 33.6 연속 자동 처리

현재는 시연 안정성을 위해 사용자 입력 시 가장 가까운 객체 한 개를 처리한다.

작업판의 모든 객체를 완전히 자동으로 연속 처리하려면 Pick 완료 확인, 이미 처리한 Target 관리, 실패 복구 등이 추가로 필요하다.

---

# 34. 개선 방향

향후 다음 기능을 추가할 수 있다.

- RGB-D Camera Depth를 이용한 3D 좌표 계산
- 실제 Link Length 기반 정확한 IK
- MoveIt 2를 이용한 충돌 회피 Motion Planning
- 작업판 여러 위치의 보정 데이터 수집
- 거리 보정값 Interpolation 또는 Regression
- Pick 성공 여부 Feedback
- Object Tracking
- 동일 객체 중복 Pick 방지
- 모든 객체 자동 연속 분류
- Dashboard에 Robot State Machine 표시
- Dashboard에서 Start / Stop 제어
- 전체 TF Tree 정리 및 검증

---

# 35. 결론

본 프로젝트에서는 USB RGB Camera, YOLO 기반 객체 인식, ArUco Marker, Homography, ROS 2, OpenManipulator-X를 이용하여 폐기물 자동 분류 Pick & Place 시스템을 구현하였다.

Camera 영상에서 PAPER, PET, CAN을 검출하고, Bounding Box의 하단 중앙점을 객체 위치 기준 Pixel로 사용하였다.

작업 영역에 배치된 ArUco Marker ID 1~4를 이용하여 Camera Pixel과 `link1` 기준 Robot XY 좌표 사이의 Homography를 계산하였다.

각 객체의 Robot XY를 계산한 뒤 Robot Origin에서의 거리를 비교하여 가장 가까운 객체를 다음 Pick Target으로 선택하였다.

실제 Robot 동작 과정에서 위치에 따라 발생하는 오차를 줄이기 위해 기준 거리보다 가까운 객체에는 음수 가중치, 먼 객체에는 양수 가중치를 적용하였다.

Robot Control은 MoveIt 2 Planning이 아닌 ROS 2 `FollowJointTrajectory` 및 `GripperCommand` Action을 이용하여 구현하였다.

또한 Robot이 물체를 집으러 가거나 분류 위치로 이동할 때 작업판 가까이에서 횡이동하지 않도록 공중 안전 자세를 적용하였다.

최종적으로 다음과 같은 ROS 2 기반 전체 Pipeline을 구성하였다.

```text
Camera
  ↓
YOLO + ArUco
  ↓
Homography
  ↓
Robot XY
  ↓
Nearest Target
  ↓
Distance Correction
  ↓
ROS 2 Topic
  ↓
Arm / Gripper Action
  ↓
OpenManipulator-X
  ↓
Pick & Place
  ↓
Dashboard / RQt / TF 결과 확인
```

이를 통해 **Vision 인식 결과가 실제 Robot 행동까지 연결되는 ROS 2 기반 폐기물 자동 분류 시스템**을 구현하였다.

---

# 36. 제출 자료 구성

최종 제출 자료에는 다음 내용을 함께 포함한다.

1. 프로젝트 계획서
2. 본 프로젝트 기술서
3. 시스템 구성도
4. Node / Topic / Action / TF 표
5. 상태 머신
6. 오류 처리표
7. Source Code
8. Model 설명
9. 실행 절차
10. RQt Graph 캡처
11. YOLO 객체 인식 결과 캡처
12. ArUco Homography 결과 캡처
13. TF `view_frames` 결과 캡처
14. Web Dashboard 캡처
15. 발표 자료
16. 시연 영상
