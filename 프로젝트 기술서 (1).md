# 프로젝트 기술서

## 1. 프로젝트 개요

본 프로젝트는 **Intel RealSense Depth Camera와 YOLOv8 기반 인공지능 객체 인식 기술을 이용하여 폐기물의 종류와 상태를 판별하고, Robotis OpenManipulator-X를 이용하여 객체를 자동으로 분류하는 시스템​**을 구현하는 것을 목적으로 한다.

대상 객체는 캔, 구겨진 종이, 플라스틱 병으로 구성하며, 단순한 객체 종류의 구분뿐만 아니라 캔과 플라스틱 병의 압착 및 손상 상태까지 인식할 수 있도록 시스템을 구성한다.

Intel RealSense Depth Camera를 통해 RGB 영상과 Depth 데이터를 동시에 획득하고, YOLOv8 기반의 객체 인식 모델을 이용하여 객체의 종류와 상태를 판단한다. 이후 Depth 정보를 이용하여 탐지된 객체의 3차원 위치를 계산하고, 해당 위치를 로봇 좌표계로 변환한다.

변환된 좌표는 ROS 2 기반의 로봇 제어 시스템으로 전달되며, MoveIt 2를 이용하여 OpenManipulator-X의 이동 경로를 계획한다. 로봇은 Gripper를 이용하여 객체를 집은 후 객체의 종류와 상태에 따라 지정된 위치에 배치한다.

---

# 2. 시스템 구성

전체 시스템은 다음과 같은 구조로 구성한다.

```text
                    Intel RealSense
                     Depth Camera
                          ↓
                RGB 영상 + Depth 데이터
                          ↓
                    OpenCV 처리
                          ↓
                    YOLOv8 모델
                          ↓
        ┌─────────────────┼─────────────────┐
        ↓                 ↓                 ↓
   Paper Model        PET Model         Can Model
        ↓                 ↓                 ↓
 crumpled paper      plastic-bottle       Intact
                    crushed-plastic-      Slightly-crushed
                       bottle              Moderately-crushed
                                          Crushed
        └─────────────────┼─────────────────┘
                          ↓
                객체 종류 / 상태 판단
                          ↓
             Recyclables Model 적용
                          ↓
             재활용 가능 여부 판단
                          ↓
                 Bounding Box 추출
                          ↓
                  중심 좌표 계산
                          ↓
             Depth 기반 3차원 위치 추정
                          ↓
                Camera → Robot 좌표변환
                          ↓
                       ROS 2
                          ↓
                      MoveIt 2
                          ↓
                 OpenManipulator-X
                          ↓
                     Gripper Pick
                          ↓
                   분류 위치로 이동
                          ↓
                    Gripper Place
```

---

# 3. 하드웨어 구성

## 3.1 Intel RealSense Depth Camera

객체 인식 및 3차원 위치 추정을 위해 **Intel RealSense Depth Camera**를 사용한다.

RealSense Depth Camera는 RGB 영상과 Depth 영상을 동시에 획득할 수 있기 때문에 객체의 시각적인 정보와 카메라로부터 객체까지의 거리 정보를 함께 활용할 수 있다.

RGB 영상은 YOLOv8 기반 객체 인식 모델의 입력으로 사용하며, Depth 데이터는 YOLO 모델이 탐지한 객체의 3차원 위치를 계산하는 데 사용한다.

이를 통해 일반적인 RGB 카메라를 사용하는 방식과 달리 객체의 깊이 정보를 활용하여 실제 공간상 위치를 추정할 수 있도록 구성한다.

---

## 3.2 Robotis OpenManipulator-X

**Robotis OpenManipulator-X**는 인식된 객체를 직접 집고 이동시키는 매니퓰레이터로 사용한다.

AI 시스템에서 계산된 객체의 3차원 위치를 기반으로 목표 위치까지 이동하며, 말단에 장착된 Gripper를 이용하여 객체를 파지한다.

객체를 파지한 이후에는 객체의 종류와 상태에 따라 지정된 분류 위치로 이동하여 객체를 배치한다.

---

## 3.3 Gripper

Gripper는 OpenManipulator-X의 말단에 장착되어 객체를 파지하고 배치하는 역할을 수행한다.

객체의 위치로 이동한 로봇은 Gripper를 닫아 객체를 파지하고, 지정된 분류 위치로 이동한 후 Gripper를 개방하여 객체를 배치한다.

---

# 4. 소프트웨어 및 개발환경

본 프로젝트의 개발환경은 다음과 같다.

| 구분 | 사용 환경 |
|---|---|
| Host OS | Windows |
| Linux 환경 | WSL2 |
| Linux 배포판 | Ubuntu |
| Robot Middleware | ROS 2 |
| Programming Language | Python |
| AI Framework | Ultralytics YOLOv8 |
| Image Processing | OpenCV |
| Robot Motion Planning | MoveIt 2 |
| Camera | Intel RealSense Depth Camera |
| Robot | Robotis OpenManipulator-X |

Windows 환경에서 **WSL2를 이용하여 Ubuntu/Linux 환경**을 구성하고, 해당 환경에서 ROS 2 기반의 로봇 제어 시스템을 구축한다.

Python은 AI 객체 인식 및 영상 처리 프로그램 구현에 사용하며, OpenCV는 RGB 영상과 Depth 데이터의 처리에 사용한다.

YOLOv8은 객체의 종류 및 상태를 인식하는 데 사용하고, ROS 2는 AI 시스템과 로봇 제어 시스템 사이의 데이터 통신을 담당한다. MoveIt 2는 OpenManipulator-X의 경로 계획 및 자세 제어에 사용한다.

---

# 5. AI 객체 인식 시스템

## 5.1 YOLOv8 기반 객체 인식

본 프로젝트에서는 폐기물의 종류와 상태를 인식하기 위해 **YOLOv8 기반 객체 탐지 모델**을 사용한다.

객체별 특성과 상태 분류 기준이 서로 다르기 때문에 하나의 모델만 사용하는 것이 아니라, **Roboflow Universe에서 제공되는 여러 모델을 목적에 따라 활용하는 다중 모델 구조**로 구성한다.

사용하는 모델은 다음과 같다.

| 구분 | 모델 ID | 이미지 수 | 주요 인식 대상 |
|---|---|---:|---|
| Paper | `crumpledpaper/1` | 약 2,800장 | 구겨진 종이 |
| PET | `plastic-bottles-ip5yb-uziag-hg1ll/1` | 약 1,200장 | 플라스틱 병 |
| Can | `can-a8pgu/2` | 약 3,300장 | 캔 및 캔 상태 |
| Recyclables | `recyclables-vs.-non-recyclables-qzeyp-aeu0v/1` | 미확인 | 재활용 가능 여부 |

각 모델은 Python 프로그램에서 다음과 같이 모델 ID를 정의하여 사용한다.

```python
PAPER_MODEL_ID = "crumpledpaper/1"
PET_MODEL_ID = "plastic-bottles-ip5yb-uziag-hg1ll/1"
CAN_MODEL_ID = "can-a8pgu/2"
MODEL_ID = "recyclables-vs.-non-recyclables-qzeyp-aeu0v/1"
```

각 모델은 입력된 RGB 영상에서 학습된 객체를 탐지하고, 탐지된 객체의 클래스와 Bounding Box 정보를 출력한다.

---

## 5.2 Paper 모델

구겨진 종이의 인식을 위해 Roboflow Universe의 다음 모델을 사용한다.

```python
PAPER_MODEL_ID = "crumpledpaper/1"
```

해당 모델은 약 **2,800장의 이미지 데이터​**를 기반으로 구성되어 있으며, RGB 영상에서 `crumpled paper` 객체를 탐지하는 데 사용한다.

탐지된 객체의 Bounding Box는 객체의 영상 내 위치를 나타내며, Bounding Box의 중심 좌표는 이후 RealSense Depth 데이터와 결합하여 객체의 3차원 위치를 계산하는 데 활용한다.

---

## 5.3 PET 모델

플라스틱 병의 인식을 위해 다음 모델을 사용한다.

```python
PET_MODEL_ID = "plastic-bottles-ip5yb-uziag-hg1ll/1"
```

해당 모델은 약 **1,200장의 이미지 데이터​**를 기반으로 구성되어 있으며, 플라스틱 병 객체를 탐지하는 데 사용한다.

플라스틱 병의 인식 결과는 다음과 같이 구분한다.

- `plastic-bottle`
- `crushed-plastic-bottle`

탐지된 객체의 Bounding Box와 중심 좌표를 이용하여 객체의 위치를 확인하고, 이후 Depth 데이터를 결합하여 3차원 위치를 계산한다.

---

## 5.4 Can 모델

캔의 인식 및 상태 구분을 위해 다음 모델을 사용한다.

```python
CAN_MODEL_ID = "can-a8pgu/2"
```

해당 모델은 약 **3,300장의 이미지 데이터​**를 기반으로 구성되어 있으며, 캔 객체의 형태 및 압착 상태를 인식하는 데 사용한다.

캔은 상태에 따라 다음과 같이 구분한다.

- `Intact`
- `Slightly-crushed`
- `Moderately-crushed`
- `Crushed`

각 상태의 인식 결과는 객체의 종류와 상태를 결정하고, 이후 로봇이 이동할 분류 위치를 결정하는 데 활용한다.

---

## 5.5 재활용 가능 여부 판별 모델

객체의 재활용 가능 여부를 판단하기 위해 다음 모델을 추가적으로 사용한다.

```python
MODEL_ID = "recyclables-vs.-non-recyclables-qzeyp-aeu0v/1"
```

해당 모델은 객체를 **재활용 가능(Recyclable)** 및 ​**재활용 불가능(Non-recyclable)**으로 구분하는 역할을 한다.

이를 통해 개별 객체의 종류 및 상태를 인식하는 것뿐만 아니라, 객체의 재활용 가능 여부를 추가적으로 판단할 수 있다.

---

# 6. YOLOv8 데이터셋 구성

YOLOv8 기반 객체 인식 모델은 Roboflow Universe에서 제공되는 데이터셋 및 모델을 활용한다.

각 객체의 특성에 따라 별도의 데이터셋과 모델을 사용하며, 각 모델은 해당 객체의 형태와 상태를 인식할 수 있도록 구성되어 있다.

사용되는 주요 데이터 규모는 다음과 같다.

```text
Paper
약 2,800 images
        ↓
crumpledpaper/1

PET
약 1,200 images
        ↓
plastic-bottles-ip5yb-uziag-hg1ll/1

Can
약 3,300 images
        ↓
can-a8pgu/2
```

각 모델에서 생성되는 객체 탐지 결과는 Bounding Box 형태로 표현되며, 이후 Depth Camera의 깊이 정보와 결합하여 객체의 3차원 위치를 추정한다.

---

# 7. 다중 YOLO 모델 기반 객체 인식 구조

본 시스템에서는 객체별로 별도의 YOLO 모델을 사용하는 다중 모델 구조를 적용한다.

하나의 RGB 영상 프레임이 입력되면 각 모델이 해당 객체를 탐지하고, 그 결과를 하나의 시스템에서 통합하여 사용한다.

```text
                         RGB 영상
                            ↓
             ┌──────────────┼──────────────┐
             ↓              ↓              ↓
        Paper Model      PET Model      Can Model
             ↓              ↓              ↓
        crumpled paper  plastic-bottle      Intact
                        crushed-plastic-    Slightly-crushed
                           bottle            Moderately-crushed
                                            Crushed
             └──────────────┼──────────────┘
                            ↓
                  객체 종류 및 상태 판단
                            ↓
                 Recyclables Model
                            ↓
                  재활용 가능 여부 판단
                            ↓
                      Bounding Box
                            ↓
                     중심 좌표 (u,v)
                            ↓
                 RealSense Depth 정보
                            ↓
                    3차원 좌표 (X,Y,Z)
```

이러한 구조를 통해 객체 종류, 객체 상태 및 재활용 가능 여부를 각각의 목적에 맞는 모델을 이용하여 판단할 수 있다.

---

# 8. Depth Camera 기반 3차원 위치 추정

YOLOv8 기반 모델은 RGB 영상에서 객체의 2차원 위치를 검출한다.

검출된 Bounding Box의 좌표를 이용하여 객체의 중심 픽셀을 계산하고, 해당 픽셀 위치의 RealSense Depth 데이터를 추출한다.

전체적인 처리 과정은 다음과 같다.

```text
YOLOv8
   ↓
Bounding Box
   ↓
(x1, y1, x2, y2)
   ↓
중심점 (u, v)
   ↓
RealSense Depth 데이터
   ↓
깊이값 Z
   ↓
3차원 좌표 (X, Y, Z)
```

Bounding Box의 중심 좌표는 다음과 같이 계산할 수 있다.

```python
u = (x1 + x2) / 2
v = (y1 + y2) / 2
```

중심 픽셀 `(u, v)`에서 Depth 값을 추출하면 카메라와 객체 사이의 거리를 확인할 수 있다.

또한 RealSense 카메라의 내부 파라미터를 이용하면 픽셀 좌표와 Depth 정보를 카메라 좌표계의 3차원 좌표 `(X, Y, Z)`로 변환할 수 있다.

이를 통해 객체의 영상 내 위치뿐만 아니라 실제 공간상 위치를 계산할 수 있다.

---

# 9. Camera-Robot 좌표 변환

Depth Camera에서 계산된 3차원 좌표는 **카메라 좌표계(Camera Coordinate System)​**를 기준으로 한다.

그러나 OpenManipulator-X는 **로봇 좌표계(Robot Coordinate System)​**를 기준으로 움직이기 때문에 두 좌표계 사이의 변환 과정이 필요하다.

전체적인 변환 과정은 다음과 같다.

```text
RealSense Camera
      ↓
Camera Coordinate
(Xc, Yc, Zc)
      ↓
Camera-Robot Calibration
      ↓
Robot Coordinate
(Xr, Yr, Zr)
      ↓
ROS 2
```

카메라와 로봇 사이의 상대적인 위치 및 방향 관계를 Calibration하여 카메라에서 계산된 객체의 3차원 위치를 로봇이 사용할 수 있는 좌표로 변환한다.

변환된 로봇 좌표는 OpenManipulator-X의 목표 위치를 결정하는 데 사용한다.

---

# 10. ROS 2 기반 시스템

ROS 2는 AI 객체 인식 시스템과 OpenManipulator-X 사이의 데이터 통신 및 로봇 제어를 담당한다.

전체적인 데이터 흐름은 다음과 같다.

```text
RealSense
    ↓
RGB / Depth
    ↓
YOLOv8
    ↓
Object Detection
    ↓
Object Class / State
    +
3D Position
    ↓
ROS 2
    ↓
MoveIt 2
    ↓
OpenManipulator-X
```

ROS 2 환경은 WSL2의 Ubuntu에서 구동하며, Python 기반 ROS 2 Node를 이용하여 카메라 데이터, AI 객체 인식 결과 및 위치 정보를 처리한다.

객체의 종류와 상태에 따라 분류 위치를 결정하고, 계산된 로봇 좌표를 기반으로 OpenManipulator-X의 목표 위치를 설정한다.

---

# 11. MoveIt 2 기반 로봇 제어

MoveIt 2는 OpenManipulator-X가 객체의 위치까지 이동하기 위한 경로를 계획하는 데 사용한다.

AI 시스템에서 객체의 위치가 결정되면 해당 위치를 로봇의 목표 위치로 설정하고, MoveIt 2를 이용하여 로봇의 이동 경로를 생성한다.

객체에 접근할 때에는 로봇과 주변 환경 및 객체 사이의 충돌을 고려하여 이동 경로를 계획한다.

객체의 위치에 도달하면 Gripper를 이용하여 객체를 파지하고, 이후 지정된 분류 위치로 이동한다.

---

# 12. Pick & Place 알고리즘

객체의 자동 분류 과정은 다음과 같이 구성한다.

```text
① RealSense RGB/Depth 영상 획득
                ↓
② YOLOv8 객체 탐지
                ↓
③ 객체 종류 및 상태 판단
                ↓
④ Bounding Box 중심점 계산
                ↓
⑤ Depth 데이터 추출
                ↓
⑥ 객체 3차원 좌표 계산
                ↓
⑦ Camera → Robot 좌표 변환
                ↓
⑧ ROS 2를 통한 목표 좌표 전달
                ↓
⑨ MoveIt 2 경로 계획
                ↓
⑩ OpenManipulator-X 이동
                ↓
⑪ Gripper Pick
                ↓
⑫ 객체별 분류 위치로 이동
                ↓
⑬ Gripper Place
```

---

# 13. 객체별 자동 분류

AI가 인식한 객체의 종류와 상태에 따라 서로 다른 분류 위치를 지정한다.

예를 들어 다음과 같은 방식으로 분류 위치를 설정할 수 있다.

| 객체 및 상태 | 분류 위치 |
|---|---|
| Intact Can | 위치 A |
| Slightly-crushed Can | 위치 B |
| Moderately-crushed Can | 위치 C |
| Crushed Can | 위치 D |
| Crumpled Paper | 위치 E |
| Plastic Bottle | 위치 F |
| Crushed Plastic Bottle | 위치 G |

YOLOv8 기반 모델이 객체의 종류와 상태를 판단하면 해당 클래스에 대응하는 목표 위치를 선택한다.

이후 OpenManipulator-X는 객체의 3차원 위치를 기반으로 객체를 Pick하고, 지정된 분류 위치로 이동하여 Place한다.

재활용 가능 여부 판단 결과는 객체의 최종 분류 조건을 결정하는 추가적인 정보로 활용할 수 있다.

---

# 14. 시스템 동작 원리

본 시스템은 크게 **객체 인식, 3차원 위치 추정, 좌표 변환, 로봇 제어​**의 네 가지 주요 과정으로 구성된다.

먼저 Intel RealSense Depth Camera를 이용하여 작업 공간의 RGB 영상과 Depth 데이터를 획득한다.

획득한 RGB 영상은 각각의 YOLOv8 모델에 입력되어 종이, 플라스틱 병, 캔 등의 객체를 탐지한다. 캔 모델은 캔의 압착 상태를 구분하며, 플라스틱 병 모델 역시 플라스틱 병의 상태를 구분한다. 또한 Recyclables 모델을 이용하여 객체의 재활용 가능 여부를 판단한다.

객체가 탐지되면 Bounding Box의 중심 픽셀을 계산하고, 해당 위치의 Depth 데이터를 추출한다. RGB 영상의 픽셀 좌표와 Depth 정보를 결합하여 카메라 기준의 3차원 위치를 계산한다.

계산된 3차원 위치는 Camera-Robot Calibration을 통해 로봇 좌표계로 변환되고, ROS 2를 통해 로봇 제어 시스템으로 전달된다.

MoveIt 2는 전달받은 목표 위치를 기반으로 OpenManipulator-X의 이동 경로를 생성한다. 로봇은 객체 위치로 이동하여 Gripper로 객체를 파지한 후, 객체의 종류와 상태에 따라 지정된 분류 위치로 이동하여 객체를 배치한다.

이를 통해 **객체의 종류 및 상태 인식 → 3차원 위치 추정 → 좌표 변환 → 로봇 Pick & Place → 자동 분류**로 이어지는 비전 기반 로봇 시스템을 구성한다.

---

# 15. 기술적 특징

## 15.1 다중 YOLO 모델을 이용한 객체 인식

본 프로젝트에서는 객체의 종류와 상태에 따라 서로 다른 YOLOv8 모델을 적용한다.

Paper, PET, Can 모델을 각각 사용함으로써 각 객체의 특성에 맞는 인식 결과를 얻을 수 있으며, 별도의 Recyclables 모델을 통해 재활용 가능 여부를 추가적으로 판단한다.

---

## 15.2 객체 상태까지 고려한 분류

단순히 캔, 종이, 플라스틱 병의 종류만 구분하는 것이 아니라 캔과 플라스틱 병의 압착 상태까지 인식한다.

특히 캔은 다음 네 가지 상태로 세분화한다.

- Intact
- Slightly-crushed
- Moderately-crushed
- Crushed

이를 통해 객체의 상태에 따른 세부적인 자동 분류가 가능하도록 구성한다.

---

## 15.3 Depth 기반 3차원 위치 추정

Intel RealSense Depth Camera를 이용하여 객체의 깊이 정보를 획득한다.

YOLOv8의 Bounding Box 정보와 Depth 데이터를 결합함으로써 객체의 2차원 위치뿐만 아니라 카메라 기준의 3차원 위치를 계산할 수 있다.

---

## 15.4 Camera-Robot 좌표계 연계

카메라 좌표계에서 계산된 객체의 3차원 위치를 Camera-Robot Calibration을 통해 로봇 좌표계로 변환한다.

이를 통해 AI가 탐지한 객체의 위치를 OpenManipulator-X의 실제 이동 목표 위치로 사용할 수 있다.

---

## 15.5 ROS 2 및 MoveIt 2 기반 로봇 제어

ROS 2를 이용하여 AI 객체 인식 결과와 로봇 제어 시스템을 연결하고, MoveIt 2를 이용하여 OpenManipulator-X의 경로를 계획한다.

이를 통해 카메라에서 획득한 객체 정보가 로봇의 실제 Pick & Place 동작으로 연결되도록 시스템을 구성한다.

---

## 15.6 자동 Pick & Place 기반 분류

객체의 종류와 상태를 인식한 후 해당 객체에 대응하는 분류 위치를 결정하고, OpenManipulator-X의 Gripper를 이용하여 객체를 자동으로 집고 배치한다.

따라서 최종 시스템은 객체의 인식부터 위치 계산, 로봇 이동, 객체 파지 및 분류까지의 작업을 자동화할 수 있도록 구성된다.

---

# 16. 전체 시스템 처리 과정

최종적인 시스템의 전체 처리 과정은 다음과 같다.

```text
                  Intel RealSense
                       ↓
             RGB + Depth 영상 획득
                       ↓
                  YOLOv8 모델
                       ↓
       ┌───────────────┼───────────────┐
       ↓               ↓               ↓
   Paper Model      PET Model       Can Model
       ↓               ↓               ↓
  구겨진 종이       플라스틱 병       캔 상태
       └───────────────┼───────────────┘
                       ↓
             Recyclables Model
                       ↓
            재활용 가능 여부 판단
                       ↓
            객체 종류 및 상태 결정
                       ↓
               Bounding Box
                       ↓
                 중심점 (u,v)
                       ↓
             Depth 값 추출
                       ↓
               3D 좌표 (X,Y,Z)
                       ↓
            Camera-Robot Calibration
                       ↓
             Robot 좌표 (X,Y,Z)
                       ↓
                    ROS 2
                       ↓
                  MoveIt 2
                       ↓
             OpenManipulator-X
                       ↓
                 Gripper Pick
                       ↓
                분류 위치 이동
                       ↓
                Gripper Place
                       ↓
                  자동 분류
```

본 시스템은 이러한 처리 구조를 통해 **AI 기반 객체 및 상태 인식과 Depth 기반 3차원 위치 추정, ROS 2 기반 로봇 제어를 결합한 자동 분류 시스템​**을 구현한다.