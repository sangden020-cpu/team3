## 5. AI 객체 인식 시스템

### 5.1 YOLOv8 기반 객체 인식

본 프로젝트에서는 폐기물의 종류와 상태를 인식하기 위해 **YOLOv8 기반 객체 탐지 모델**을 사용한다.

객체별 특성과 상태가 서로 다르기 때문에 하나의 모델만 사용하는 것이 아니라, **Roboflow Universe에서 제공되는 여러 객체 인식 모델을 활용하여 종이, 플라스틱 병, 캔 및 재활용 가능 여부를 판단하는 구조​**로 구성하였다.

사용한 모델은 다음과 같다.

| 구분 | 모델 ID | 데이터 이미지 수 | 주요 인식 대상 |
|---|---|---:|---|
| Paper | `crumpledpaper/1` | 약 2,800장 | 구겨진 종이 |
| PET | `plastic-bottles-ip5yb-uziag-hg1ll/1` | 약 1,200장 | 플라스틱 병 |
| Can | `can-a8pgu/2` | 약 3,300장 | 캔 및 캔 상태 |
| Recyclables | `recyclables-vs.-non-recyclables-qzeyp-aeu0v/1` | - | 재활용 가능/불가능 객체 |

Python 프로그램에서는 각 모델을 식별하기 위해 다음과 같이 모델 ID를 정의하여 사용한다.

```python
PAPER_MODEL_ID = "crumpledpaper/1"
PET_MODEL_ID = "plastic-bottles-ip5yb-uziag-hg1ll/1"
CAN_MODEL_ID = "can-a8pgu/2"
MODEL_ID = "recyclables-vs.-non-recyclables-qzeyp-aeu0v/1"
```

각 모델은 입력된 RGB 영상에서 학습된 객체를 탐지하고, 탐지된 객체의 클래스와 Bounding Box 정보를 출력한다.

---

### 5.2 Paper 모델

구겨진 종이의 인식을 위해 Roboflow Universe의 다음 모델을 사용한다.

```python
PAPER_MODEL_ID = "crumpledpaper/1"
```

해당 모델은 약 **2,800장의 이미지 데이터​**를 기반으로 구성되어 있으며, RGB 영상에서 구겨진 종이 객체를 탐지하는 데 사용한다.

탐지된 Bounding Box의 위치 정보는 이후 Depth Camera의 깊이 데이터와 결합하여 객체의 3차원 위치를 계산하는 데 활용한다.

---

### 5.3 PET 모델

플라스틱 병의 인식을 위해 다음 모델을 사용한다.

```python
PET_MODEL_ID = "plastic-bottles-ip5yb-uziag-hg1ll/1"
```

해당 모델은 약 **1,200장의 이미지 데이터​**를 기반으로 구성되어 있으며, 플라스틱 병 객체를 탐지하는 데 사용한다.

플라스틱 병이 탐지되면 Bounding Box를 이용하여 객체의 영상상 위치를 확인하고, Depth 데이터를 결합하여 로봇의 Pick 위치를 계산한다.

---

### 5.4 Can 모델

캔의 인식 및 상태 구분을 위해 다음 모델을 사용한다.

```python
CAN_MODEL_ID = "can-a8pgu/2"
```

해당 모델은 약 **3,300장의 이미지 데이터​**를 기반으로 구성되어 있으며, 캔 객체의 형태와 상태를 인식하는 데 사용한다.

캔은 상태에 따라 다음과 같이 구분한다.

- `Intact`
- `Slightly-crushed`
- `Moderately-crushed`
- `Crushed`

각 상태에 대한 인식 결과는 객체의 분류 위치를 결정하는 데 활용한다.

---

### 5.5 재활용 가능 여부 판별 모델

전체 객체의 재활용 가능 여부를 판단하기 위해 다음 모델을 추가적으로 사용한다.

```python
MODEL_ID = "recyclables-vs.-non-recyclables-qzeyp-aeu0v/1"
```

해당 모델은 객체를 **재활용 가능(Recyclable)** 및 ​**재활용 불가능(Non-recyclable)**으로 구분하는 데 사용한다.

이를 통해 개별 객체의 종류와 상태를 인식하는 것뿐만 아니라, 객체의 재활용 가능 여부를 판단하는 기능을 추가할 수 있다.

---

### 5.6 다중 YOLO 모델 기반 객체 인식 구조

본 시스템은 여러 개의 YOLO 모델을 목적에 따라 구분하여 사용하는 구조로 구성한다.

```text
                         RGB 영상
                            ↓
             ┌──────────────┼──────────────┐
             ↓              ↓              ↓
        Paper Model      PET Model      Can Model
             ↓              ↓              ↓
        구겨진 종이       플라스틱 병       캔 상태
             │              │              │
             └──────────────┼──────────────┘
                            ↓
                   객체 종류 및 상태 판단
                            ↓
                Recyclables Model
                            ↓
                 재활용 가능 여부 판단
                            ↓
                     Bounding Box
                            ↓
                  중심 좌표 (u, v)
                            ↓
               RealSense Depth 데이터
                            ↓
                    3차원 좌표 (X,Y,Z)
                            ↓
                    로봇 좌표계 변환
                            ↓
                       ROS 2 / MoveIt 2
                            ↓
                  OpenManipulator-X
```

이와 같이 객체의 종류와 상태를 판단하는 모델과 재활용 가능 여부를 판단하는 모델을 분리하여 사용함으로써 각 모델의 목적에 맞는 객체 인식 결과를 얻을 수 있도록 구성한다.

---

### 5.7 모델 적용 방식

각 모델의 탐지 결과는 단독으로 사용하는 것이 아니라 전체 자동 분류 시스템의 입력 데이터로 활용한다.

객체가 카메라 영상에 나타나면 각 YOLO 모델을 통해 객체를 탐지하고, 탐지 결과에서 얻어진 **객체 종류, 상태, Bounding Box** 정보를 종합한다.

이후 Bounding Box의 중심 좌표와 RealSense Depth 데이터를 결합하여 객체의 3차원 위치를 계산하고, 해당 위치를 로봇 좌표계로 변환한다.

최종적으로 객체의 종류와 상태에 따라 분류 위치를 결정하고 OpenManipulator-X가 Pick & Place 동작을 수행하도록 구성한다.