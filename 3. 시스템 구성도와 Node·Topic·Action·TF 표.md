# 비전 기반 재활용 쓰레기 자동 분류 Pick & Place 시스템

> OpenManipulator-X + ArUco Homography + Roboflow 3-Model Detection

---

## 1. 시스템 구성도와 Node·Topic·Action·TF 표

### 1.1 시스템 구성도

~~~mermaid
flowchart TB
    CAM["USB Camera<br/>/dev/video0<br/>MJPEG 640x480@30"]

    subgraph N1["1. aruco_homography_calibration (GUI + 카메라 드라이버)"]
        CAP["프레임 캡처<br/>+ FPS 측정"]
        ARUCO["ArUco 검출<br/>ID 1~4"]
        HOMO["Homography 계산·LOCK<br/>픽셀→로봇좌표 변환<br/>거리 정렬→nearest 선정"]
    end

    subgraph N2["2. waste_detector (GUI)"]
        POOL["ThreadPoolExecutor (3 스레드)<br/>paper / pet / can 모델<br/>→ bbox 밑점 JSON"]
    end

    subgraph N3["3. waste_pick_node (CLI)"]
        CTRL["목표 수신·신선도 검사<br/>작업영역 검증<br/>거리 가중 보정<br/>근사 IK + 12단계 시퀀스"]
    end

    ARM["/arm_controller<br/>follow_joint_trajectory"]
    GRIP["/gripper_controller<br/>gripper_cmd"]
    HW["OpenManipulator-X<br/>(Dynamixel XM430 x5)"]

    CAM -- "GStreamer<br/>(v4l2src→jpegdec→BGR)" --> CAP
    CAP --> ARUCO --> HOMO

    N1 -- "/detect_trash/image_raw<br/>(Image, 30Hz)" --> N2
    N2 -- "/detect_trash/detections<br/>(String JSON)" --> N1
    N1 -- "/detect_trash/nearest_target<br/>(String JSON)" --> N3
    N1 -. "/detect_trash/target_point<br/>(PointStamped)" .-> RVIZ["RViz 등"]

    N3 -- "Action Goal" --> ARM
    N3 -- "Action Goal" --> GRIP
    ARM -- "ros2_control" --> HW
    GRIP -- "ros2_control" --> HW
~~~

**데이터 흐름 요약**: 카메라 → 1번 노드 영상 발행 → 2번 노드 추론(픽셀 좌표) → 1번 노드 Homography 변환·최근접 선정 → 3번 노드 파지 실행

### 1.2 Node 표

| Node | 파일 | 역할 | 실행 형태 |
|---|---|---|---|
| `aruco_homography_calibration` | `aruco_homography_calibration.py` | 카메라 드라이버 + ArUco 캘리브레이션 + 좌표 변환 + 최근접 목표 선정 | GUI 루프(자체 while) + `spin_once` |
| `waste_detector` | `detector.py` | 3-모델 병렬 추론, bbox 밑점 추출, 결과 발행 | `rclpy.spin` + 30 Hz 타이머 + GUI |
| `waste_pick_node` | `robot_control.py` | 목표 검증·보정, 근사 IK, 12단계 Pick & Place | 별도 spin 스레드 + CLI input 루프 |
| `/arm_controller` | ros2_control | 4관절 궤적 추종 | — |
| `/gripper_controller` | ros2_control | 그리퍼 위치/힘 제어 | — |
| `controller_manager`, `robot_state_publisher` | 브링업 패키지 | 컨트롤러 관리 / TF 발행 | — |

### 1.3 Topic 표

| Topic | 타입 | Publisher → Subscriber | QoS | 내용 |
|---|---|---|---|---|
| `/detect_trash/image_raw` | `sensor_msgs/Image` (bgr8) | 1 → 2 | sensor_data (Best Effort) | 카메라 원본, `frame_id=trash_camera`, 실측 FPS 로그 |
| `/detect_trash/detections` | `std_msgs/String` (JSON) | 2 → 1 | depth 10 | 전체 검출 목록(픽셀 좌표) |
| `/detect_trash/target_point` | `geometry_msgs/PointStamped` | 1 → (RViz 등) | depth 10 | 최근접 객체 로봇 좌표, `frame_id=link1` |
| `/detect_trash/nearest_target` | `std_msgs/String` (JSON) | 1 → 3 | depth 10 | 최근접 객체(종류+좌표+거리) |
| `/joint_states`, `/tf`, `/tf_static` | 표준 | 브링업 → 전체 | — | 로봇 상태·변환 |

#### 1.3.1 JSON 메시지 스키마

**`/detect_trash/detections`** (배열)

| 필드 | 타입 | 설명 |
|---|---|---|
| `type` | string | `paper` / `pet` / `can` |
| `class` | string | 모델의 원본 클래스명 |
| `confidence` | float | 임계값 통과한 confidence |
| `x1, y1, x2, y2` | int | bbox 픽셀 좌표 |
| `center_x, center_y` | int | **밑변 중앙점** (기준점) |

**`/detect_trash/nearest_target`** (단일 객체): 위 필드 + `pixel_x`, `pixel_y`, `x`, `y`(로봇 좌표 m), `distance`(m)

### 1.4 Action 표

| Action | 타입 | Client → Server | 호출 방식 | 비고 |
|---|---|---|---|---|
| `/arm_controller/follow_joint_trajectory` | `control_msgs/FollowJointTrajectory` | 3 → arm_controller | `send_goal_async` + `time.sleep(duration+0.3)` | 결과 미확인(fire-and-forget) — 3.4 참조 |
| `/gripper_controller/gripper_cmd` | `control_msgs/GripperCommand` | 3 → gripper_controller | `send_goal_async` + `time.sleep(1.2)` | 동일 |

### 1.5 TF / 프레임 표

| 프레임 | 발행 주체 | 용도 |
|---|---|---|
| `link1` | robot_state_publisher (URDF) | **로봇 평면 좌표계 기준** — target_point의 frame_id |
| `trash_camera` | 1번 노드의 Image header | 카메라 프레임 식별용 (정식 TF 미발행) |
| `base_link` ~ `end_effector_link` | robot_state_publisher | 로봇 링크 체인 |

> **참고**: 본 시스템은 카메라 외부 파라미터 TF 대신 **Homography가 카메라↔로봇 평면 변환을 대체**한다.
> 3D TF 체인이 필요 없다는 것이 장점이지만, 평면 위 물체에만 유효하다는 제약이 있다.

---

## 2. 상태 머신과 오류 처리표

### 2.1 캘리브레이션 노드 상태 머신

~~~mermaid
stateDiagram-v2
    [*] --> CAMERA_INIT
    CAMERA_INIT --> COLLECTING : 카메라 열기 성공
    CAMERA_INIT --> [*] : 실패 (RuntimeError 종료)

    COLLECTING --> COLLECTING : 매 프레임 ArUco 검출<br/>ID 1~4 위치 저장<br/>0.5s 미검출 마커 제거
    COLLECTING --> LOCKED : ID 1~4 동시 확보<br/>→ H 계산 후 고정

    LOCKED --> LOCKED : detections 수신 → 좌표 변환<br/>거리 정렬 → nearest 발행<br/>마우스 클릭 → 좌표 검증
    LOCKED --> COLLECTING : 'r' 키 (reset)

    LOCKED --> [*] : 'q' / 카메라 read 실패
    COLLECTING --> [*] : 'q' / 카메라 read 실패
~~~

### 2.2 검출 노드 파이프라인 상태 (모델별 독립)

~~~mermaid
stateDiagram-v2
    direction LR
    [*] --> IDLE
    IDLE --> INFERRING : 최신 프레임 존재<br/>& future is None
    INFERRING --> IDLE : future.done<br/>→ 결과 수거 (예외 시 빈 목록)
~~~

> 30 Hz 타이머: 결과 수거 → 재추론 시작 → 시각화 → JSON 발행
> paper / pet / can 3개 모델이 각각 위 상태를 **독립적으로** 순환한다.

### 2.3 Pick & Place 시퀀스 상태 머신 (waste_pick_node)

~~~mermaid
stateDiagram-v2
    [*] --> WAIT_CMD
    WAIT_CMD --> GET_TARGET : Enter 입력
    GET_TARGET --> WAIT_CMD : 목표 없음 / 2s 초과 / 영역 밖<br/>(warn 후 복귀)
    GET_TARGET --> S1 : 목표 유효<br/>→ 거리 가중 보정

    S1  : [S1] GRIPPER_OPEN (0.019m)
    S2a : [S2a] 정면 안전 자세 상승 (joint1=0)
    S2b : [S2b] 공중 회전 → 물체 방향
    S2c : [S2c] 물체 상공 이동 (Z_SAFE=0.30)
    S3  : [S3] 하강 (PICK_Z[type])
    S4  : [S4] GRIPPER_HOLD (0.003m) — 파지
    S5  : [S5] +5cm 리프트
    S6  : [S6] 안전 비행 자세 상승
    S7  : [S7] 공중 회전 → 분류함 방향
    S8  : [S8] 분류함 하강
    S9  : [S9] GRIPPER_OPEN — 배치
    S10 : [S10] 안전 자세 상승
    S11 : [S11] 공중 회전 → 정면 복귀
    S12 : [S12] HOME 복귀 [0, -1.05, 0.35, 0.70]

    S1 --> S2a
    S2a --> S2b
    S2b --> S2c
    S2c --> S3
    S3 --> S4
    S4 --> S5
    S5 --> S6
    S6 --> S7
    S7 --> S8
    S8 --> S9
    S9 --> S10
    S10 --> S11
    S11 --> S12
    S12 --> WAIT_CMD

    note right of S4 : S1~S10 어느 단계든<br/>서버 미응답(False) 시<br/>시퀀스 중단 → WAIT_CMD
~~~

### 2.4 오류 처리표

#### 1. aruco_homography_calibration

| 오류 | 감지 조건 | 처리 | 복구 |
|---|---|---|---|
| 카메라 열기 실패 | `cap.isOpened() == False` | RuntimeError → 노드 종료 | 장치 경로/GStreamer 플러그인 확인 |
| 프레임 read 실패 | `ret == False` | 에러 로그 후 루프 탈출·종료 | 케이블/전원 확인 후 재실행 |
| 마커 순간 미검출 | 검출 목록에 없음 | 0.5 s간 이전 위치 유지 (hold) | 자동 |
| 마커 장기 미검출 | hold 시간 초과 | 후보에서 제거, 미LOCK 상태 유지 | 마커 재노출 |
| 캘리브레이션 오염(마커 이동 등) | 사용자 판단 | — | `r` 키로 reset → 재수집 |
| Detection JSON 파싱 실패 | `JSONDecodeError` | 에러 로그, 해당 메시지 폐기 | 자동(다음 메시지) |
| 좌표 필드 결손/형변환 실패 | `KeyError` 등 | 해당 객체만 skip | 자동 |
| 미지원 type | can/pet/paper 외 | 해당 객체 필터링 | — |
| H 미계산 상태 클릭 | `H is None` | "NOT READY" 안내 출력 | 마커 인식 후 재시도 |
| H 역행렬 불가 | `LinAlgError` | 작업영역 표시만 생략 | reset 후 재캘리브레이션 |
| Image 발행 예외 | cv_bridge 오류 | 에러 로그, 해당 프레임 skip | 자동 |

#### 2. waste_detector

| 오류 | 감지 조건 | 처리 | 복구 |
|---|---|---|---|
| API Key 미설정 | 환경변수 없음 | RuntimeError + 설정 명령 안내 | `export ROBOFLOW_API_KEY=...` |
| 모델 로딩 실패 | `get_model` 예외 | 기동 실패 | 모델 ID/네트워크 확인 |
| 인코딩 불일치 | `encoding != 'bgr8'` | 에러 로그, 프레임 폐기 | 발행측 확인 |
| Image 버퍼 변환 실패 | reshape 예외 | 에러 로그, 프레임 폐기 | 자동 |
| 추론 예외 (네트워크 등) | `infer` 예외 | 해당 모델만 빈 결과 반환 — 타 모델 영향 없음 | 자동 재시도(다음 주기) |
| Future 결과 예외 | `future.result` 예외 | 해당 모델 결과 빈 목록으로 초기화 | 자동 |
| 낮은 confidence | threshold 미달 | 해당 검출 제외 | — |

#### 3. waste_pick_node

| 오류 | 감지 조건 | 처리 | 복구 |
|---|---|---|---|
| 목표 없음 | target 필드 None | warn 후 명령 대기 복귀 | 물체 배치/인식 확인 |
| 목표 stale | 수신 후 2.0 s 초과 | 목표 무효 처리 | 자동(새 목표 수신) |
| 작업영역 밖 | X∉[0, 0.4] 또는 Y∉[−0.2, 0.2] | callback에서 저장 자체를 거부 | 물체 재배치 |
| 분류 위치 미설정 | `BIN_POSES` 없음 | 에러 로그, 중단 | 좌표 등록 |
| Action 서버 미응답 | `wait_for_server` 2 s 초과 | False → 시퀀스 즉시 중단 | 컨트롤러 재기동 |
| 목표 JSON 파싱 실패 | 예외 | 조용히 폐기 | 자동 |

#### 시스템 수준 알려진 한계 (개선 권장 사항)

| # | 한계 | 영향 | 개선안 |
|---|---|---|---|
| 1 | Action 결과 미확인 (`send_goal_async` + sleep) | 궤적 실패·goal 거부를 감지 못하고 다음 단계 진행 | `spin_until_future_complete`로 goal 수락·result·error_code 검사 |
| 2 | 파지 성공 여부 미검증 | 빈 그리퍼로 S5~S9 진행 가능 | GripperCommand result의 `reached_goal` 검사, 실패 시 재시도 |
| 3 | 근사 IK의 자세·범위 한계 | 작업영역 가장자리·paper의 z=−0.07에서 오차 증대 | 해석적 IK로 교체 또는 구간별 보정 테이블 확대 |
| 4 | Homography 평면 가정 | 높이가 큰 물체는 밑점이라도 잔여 오차 존재 | 물체별 높이 오프셋, 또는 카메라 자세 보정 |
| 5 | 실행 중 목표 갱신 미반영 | 시퀀스 시작 시점 좌표로 고정 | 하강 직전 1회 재확인 추가 |
| 6 | BIN 좌표(−0.18, ±0.13)는 근사 IK 학습 범위 밖(후방) | place 정확도 별도 실측 필요 | 분류함 자세를 관절각으로 직접 티칭 |
| 7 | 캘리브레이션 노드에 GUI·카메라·변환이 집중 | 헤드리스 환경 실행 불가 | 표시 기능 분리(파라미터로 on/off) |

---

## 3. Source Code, Launch, Config와 Model 설명

### 3.1 패키지 구조

~~~text
waste_sorting/
├── package.xml
├── setup.py
├── config/
│   └── waste_sorting_params.yaml
├── launch/
│   └── waste_sorting.launch.py
├── waste_sorting/
│   ├── __init__.py
│   ├── aruco_homography_calibration.py   # 노드 1
│   ├── detector.py                       # 노드 2
│   └── robot_control.py                  # 노드 3
├── markers/
│   └── aruco_4x4_50_id1~4.pdf            # 인쇄용 마커
└── README.md
~~~

### 3.2 소스 코드 상세 설명

#### 3.2.1 `aruco_homography_calibration.py`

| 구성 요소 | 함수/메서드 | 설명 |
|---|---|---|
| 카메라 | `gstreamer_pipeline()` | v4l2src → MJPEG 디코딩 → BGR appsink. `drop=true max-buffers=1`로 항상 최신 프레임만 유지(지연 누적 방지) |
| 영상 발행 | `publish_image()` | cv_bridge 변환, 1초 단위 실측 발행 FPS 로그 |
| 좌표 변환 | `detection_callback()` | JSON 파싱 → `perspectiveTransform` → type 필터 → 거리 정렬 → target_point + nearest_target 발행, 0.5 s 간격 우선순위 표 출력 |
| 검증 도구 | `mouse_callback()` | 화면 클릭 지점의 로봇 좌표를 m/cm로 출력 — 캘리브레이션 정확도 현장 검증용 |
| 마커 관리 | `get_marker_center()` / `remove_old_markers()` | 4코너 평균 중심 / 0.5 s hold 후 제거 |
| 캘리브레이션 | `calculate_homography()` | ID 1~4 확보 시 1회 계산 후 LOCK |
| 시각화 | `draw_saved_markers/workspace/camera_axis()` | 마커·작업영역 폴리곤·원점(H 역행렬 역투영)·픽셀 축 표시 |
| 리셋 | `reset_calibration()` | H·마커 캐시 초기화 |
| 메인 루프 | `run()` | 캡처→발행→검출→계산→표시→`spin_once(0.0)` 비블로킹 통합 루프 |

#### 3.2.2 `detector.py`

| 구성 요소 | 함수/메서드 | 설명 |
|---|---|---|
| 영상 수신 | `image_callback()` | `np.frombuffer` + step 고려 reshape (cv_bridge 미사용), EMA 방식 FPS 계산(0.9/0.1) |
| 결과 변환 | `convert_result()` | supervision 파싱 → threshold 필터 → 밑변 중앙점 계산 → dict 목록 |
| 추론 실행 | `run_paper/pet/can()` | 모델별 추론 + 예외 시 빈 목록 (모델 간 격리) |
| 비동기 관리 | `update_inference_results()` / `start_inference()` | 완료 Future 수거 / idle 모델에 최신 프레임 제출 — 모델별 독립 주기 |
| 시각화 | `draw_predictions()` | bbox + 기준점(빨간 원) + 픽셀 좌표 + 클래스/confidence 라벨 |
| 발행 | `publish_detections()` | 3종 결과 병합 JSON 발행 (30 Hz — 추론 미완료 시 직전 결과 재발행) |
| 정리 | `cleanup()` | executor shutdown(cancel_futures) + 창 정리 |

#### 3.2.3 `robot_control.py`

| 구성 요소 | 함수/메서드 | 설명 |
|---|---|---|
| 목표 수신 | `target_callback()` | JSON 파싱 → type/작업영역 검증 → lock으로 스레드 안전 저장 + 수신 시각 기록 |
| 목표 조회 | `get_current_target()` | 2 s 신선도 검사 통과분만 반환 |
| 좌표 보정 | `apply_distance_weight()` | 기준거리 0.308 m 대비 near/far 비대칭 선형 가중 (−30% ~ +15%) |
| 자세 계산 | `get_safe_pose()` | joint1 임의 + 비행자세(−35°, −30°) + joint4 수평 유지 |
| 근사 IK | `simple_ik_solver()` | 어깨 높이 0.25 m 기준 선형 근사식 |
| 팔 제어 | `send_arm_trajectory()` | 단일 waypoint goal 비동기 송신 + sleep 동기화 |
| 그리퍼 | `control_gripper()` | position + max_effort 100 |
| 시퀀스 | `execute_pick_sequence()` | 12단계 실행 (3.3 상태 머신) |
| 메인 | `main()` | spin 전용 데몬 스레드 분리 — input() 블로킹과 콜백 수신 동시 처리 |

##### 근사 IK 수식

~~~text
joint1 = atan2(y, x)
joint2 = -0.20 + (0.25 - z) x 2.2
joint3 =  0.50 - (r - 0.22) x 2.0 + (0.25 - z) x 1.0
joint4 = -(joint2 + joint3)        <- 그리퍼 항상 수평
~~~

### 3.3 Launch 파일

~~~python
# launch/waste_sorting.launch.py
from launch import LaunchDescription
from launch_ros.actions import Node


def generate_launch_description():
    return LaunchDescription([

        # 노드 1: 카메라 + 캘리브레이션 (GUI 필요)
        Node(
            package='waste_sorting',
            executable='aruco_homography_calibration',
            name='aruco_homography_calibration',
            output='screen',
        ),

        # 노드 2: 객체 인식 (GUI + ROBOFLOW_API_KEY 필요)
        Node(
            package='waste_sorting',
            executable='detector',
            name='waste_detector',
            output='screen',
            # additional_env={'ROBOFLOW_API_KEY': '...'},
        ),

        # 노드 3: 로봇 제어 (input() 사용 -> emulate_tty 필수)
        Node(
            package='waste_sorting',
            executable='robot_control',
            name='waste_pick_node',
            output='screen',
            emulate_tty=True,
        ),
    ])
~~~

#### 실행 절차 (터미널 분리 — 권장)

~~~bash
# 터미널 0: 로봇 브링업 (하드웨어 + 컨트롤러)
ros2 launch open_manipulator_x_bringup hardware.launch.py

# 터미널 1: 카메라 + 캘리브레이션
ros2 run waste_sorting aruco_homography_calibration
#  -> ArUco ID 1~4 인식 -> "Calibration LOCK" 확인
#  -> 화면 클릭으로 좌표 정확도 검증 (권장)

# 터미널 2: 객체 인식
export ROBOFLOW_API_KEY='YOUR_KEY'
ros2 run waste_sorting detector

# 터미널 3: 로봇 제어
ros2 run waste_sorting robot_control
#  -> 우선순위 표에서 NEXT TARGET 확인 -> Enter로 1개 처리
~~~

> **기동 순서 의존성**: 브링업 → 1 → 2 → 3
> (1번 노드가 카메라를 소유하므로 반드시 2번보다 먼저. 3번은 nearest_target 수신 전까지 대기하므로 순서 유연)

#### 조작 키

| 대상 | 키 | 동작 |
|---|---|---|
| 캘리브레이션 창 | `r` | 캘리브레이션 리셋 (재수집) |
| 캘리브레이션 창 | `q` | 종료 |
| 캘리브레이션 창 | 마우스 좌클릭 | 클릭 지점 로봇 좌표 출력 |
| 검출 창 | `q` | 종료 |
| 로봇 제어 CLI | `Enter` | 가장 가까운 객체 1개 Pick & Place |
| 로봇 제어 CLI | `q` | 종료 |

### 3.4 Config 파일 (`waste_sorting_params.yaml`)

> 현재 코드는 값이 하드코딩되어 있으며, 아래는 파라미터 외부화 시의 목표 구조이다.

~~~yaml
aruco_homography_calibration:
  ros__parameters:
    camera_device: "/dev/video0"
    image_width: 640
    image_height: 480
    fps: 30
    target_ids: [1, 2, 3, 4]
    marker_hold_sec: 0.5
    # 작업영역 마커 로봇 좌표 [x, y] (ID 1->4 순)
    robot_points: [0.4, 0.2,  0.4, -0.2,  0.0, -0.2,  0.0, 0.2]

waste_detector:
  ros__parameters:
    paper_model_id: "crumpledpaper/1"
    pet_model_id: "plastic-bottles-ip5yb-uziag-hg1ll/1"
    can_model_id: "can-a8pgu/2"
    paper_threshold: 0.70
    pet_threshold: 0.85
    can_threshold: 0.75

waste_pick_node:
  ros__parameters:
    workspace: {x_min: 0.0, x_max: 0.4, y_min: -0.2, y_max: 0.2}
    target_timeout: 2.0
    pick_z: {can: 0.02, pet: 0.02, paper: -0.07}
    z_safe: 0.30
    bin_poses:
      can:   [-0.18, -0.13, 0.05]
      pet:   [-0.18,  0.00, 0.05]
      paper: [-0.18,  0.13, 0.05]
    safe_flight: {joint2_deg: -35.0, joint3_deg: -30.0}
    gripper: {open: 0.019, hold: 0.003, effort: 100.0}
    distance_weight:
      reference_distance: 0.30806   # sqrt(0.30^2 + 0.07^2)
      near_gain: 3.0
      far_gain: 1.5
      min_weight: -0.30
      max_weight: 0.15
~~~

### 3.5 Model 설명

#### 3.5.1 딥러닝 모델 (Roboflow Inference)

| 항목 | Paper | PET | Can |
|---|---|---|---|
| Model ID | `crumpledpaper/1` | `plastic-bottles-ip5yb-uziag-hg1ll/1` | `can-a8pgu/2` |
| 태스크 | Object Detection | Object Detection | Object Detection |
| Confidence 임계값 | 0.70 | **0.85** (오탐 많아 높게 설정) | 0.75 |
| 실행 | Roboflow `inference` 라이브러리 (API Key 필요) | 동일 | 동일 |
| 병렬화 | 전용 스레드 1개, 완료 즉시 최신 프레임으로 재추론 | 동일 | 동일 |

#### 3.5.2 ArUco 마커 사양

| 항목 | 값 |
|---|---|
| Dictionary | `DICT_4X4_50` |
| 사용 ID | 1, 2, 3, 4 (작업영역 네 꼭짓점) |
| 마커 배치 | ID1 (0.4, 0.2) 오른쪽 아래 / ID2 (0.4, -0.2) 왼쪽 아래 / ID3 (0.0, -0.2) 왼쪽 위 / ID4 (0.0, 0.2) 오른쪽 위 |
| 코너 정밀화 | `CORNER_REFINE_SUBPIX` |
| 검출 강건화 | adaptiveThreshWinSize 3~53 (step 4), minMarkerPerimeterRate 0.005 |
| 배치 정확도 요건 | 마커 **중심**이 robot_points 좌표에 오도록 부착 (마커 크기는 H에 영향 없음, 중심 위치만 중요) |

#### 3.5.3 로봇 모델

| 항목 | 내용 |
|---|---|
| 로봇 | OpenManipulator-X (4-DOF + 1-DOF 그리퍼, Dynamixel XM430 x5) |
| 제어 인터페이스 | ros2_control — `joint_trajectory_controller`(arm), `gripper_action_controller` |
| 운동학 모델 | URDF 기반 정밀 IK 대신 경험적 근사 IK + 실측 거리 보정 채택 (계산 단순성 우선, 정밀도는 보정으로 보완) |
| Home 자세 | `[0, -1.05, 0.35, 0.70]` rad |
| 안전 비행 자세 | `[q1, -35deg, -30deg, +65deg]` — 그리퍼 수평 유지 |

---

## 부록: 실험/검증 체크리스트

- [ ] Homography 정확도: 화면 클릭 vs 실측 좌표 오차 표 (예: 9개 지점)
- [ ] 거리 보정 파라미터 도출 과정: 보정 전/후 파지 성공률
- [ ] 모델별 confidence 임계값 선정 근거 (오탐/미탐 통계)
- [ ] 12단계 시퀀스 소요 시간 (1사이클 총 시간 실측)
- [ ] 종류별 파지 성공률 (can/pet/paper 각 N회 시험)
