# 프로젝트 기술서

# RGB 카메라와 ArUco 기반 폐기물 자동 분류 및 Pick & Place 시스템

---

## 1. 프로젝트 개요

본 프로젝트는 RGB 카메라와 YOLOv8 기반 객체 인식 기술을 이용하여 폐기물의 종류와 위치를 판단하고, Robotis OpenManipulator-X를 이용하여 폐기물을 자동으로 집어 지정된 위치에 분류하는 시스템을 구축하는 것을 목적으로 한다.

기존의 Depth Camera를 이용한 거리 측정 방식 대신, USB RGB 카메라와 작업판에 설치된 ArUco Marker를 이용하여 폐기물의 위치를 계산하는 방식을 적용한다.

작업판의 네 모서리에 4개의 ArUco Marker를 배치하고, 각 Marker의 위치를 기준으로 작업판의 XY 좌표 평면을 구성한다.

카메라 영상에서 ArUco Marker를 검출하면 작업판의 위치와 방향을 파악할 수 있으며, 이를 기준으로 카메라 영상의 픽셀 좌표를 작업판의 실제 XY 좌표로 변환한다.

폐기물은 YOLOv8 기반 객체 탐지 모델을 이용하여 인식한다.

YOLO가 폐기물을 탐지하면 해당 객체의 Bounding Box를 생성하고, Bounding Box의 하단 중앙점(Bottom Center Point)을 해당 폐기물을 집기 위한 Pick Point로 사용한다.

이 Pick Point의 영상 좌표를 Homography 변환을 이용하여 작업판의 XY 좌표로 변환하고, 변환된 좌표를 OpenManipulator-X의 Pick 목표 위치로 사용한다.

계산된 목표 좌표는 ROS 2를 통해 로봇 제어 시스템으로 전달되며, MoveIt 2를 이용하여 OpenManipulator-X의 이동 경로를 계획한다.

로봇은 폐기물의 위치로 이동하여 Gripper를 이용해 폐기물을 집은 후, 폐기물의 종류에 따라 지정된 분류 위치로 이동하여 폐기물을 배치한다.

---

# 2. 시스템 전체 구성

```text
                    USB RGB Camera
                           │
                           ↓
                    OpenCV 영상 처리
                           │
             ┌─────────────┴─────────────┐
             │                           │
             ↓                           ↓
       ArUco Marker                  YOLOv8
          검출                     객체 탐지
             │                           │
             ↓                           ↓
       작업판 4개 모서리              폐기물 검출
       기준점 획득                       │
             │                           ↓
             ↓                     Bounding Box
      작업판 XY 좌표계                    │
          구성                           ↓
                              Bottom Center Point
                                      │
             └─────────────┬─────────────┘
                           ↓
                  Homography 변환
                           ↓
                    작업판 XY 좌표
                           ↓
                  로봇 목표 위치 결정
                           ↓
                         ROS 2
                           ↓
                       MoveIt 2
                           ↓
                  OpenManipulator-X
                           ↓
                      Gripper Pick
                           ↓
                   분류 위치로 이동
                           ↓
                    Gripper Place
```

---

# 3. 하드웨어 구성

## 3.1 USB RGB Camera

본 시스템의 영상 입력 장치로 USB RGB Camera를 사용한다.

USB RGB Camera는 작업판과 그 위에 놓인 폐기물을 촬영하며, 획득한 RGB 영상을 OpenCV를 통해 처리한다.

하나의 카메라 영상에서 다음 두 가지 작업을 수행한다.

1. ArUco Marker 검출
2. YOLOv8 기반 폐기물 객체 검출

본 시스템에서는 Depth Camera를 사용하지 않으며, RGB 영상만을 이용하여 폐기물의 위치를 계산한다.

카메라는 다음과 같은 정보를 제공한다.

- 작업판 영상
- ArUco Marker의 영상 좌표
- 폐기물의 Bounding Box
- 폐기물 Pick Point의 픽셀 좌표

---

## 3.2 작업판

폐기물이 놓이는 평면 형태의 작업판을 구성한다.

작업판은 폐기물의 배치 영역이면서 동시에 로봇의 Pick 작업이 이루어지는 영역이다.

작업판의 네 모서리에 ArUco Marker를 배치하여 작업판의 기준점을 구성한다.

작업판의 한 모서리를 원점으로 설정하고 작업판의 가로 방향과 세로 방향을 각각 X축과 Y축으로 설정한다.

```text
                    Y
                    ↑
                    │
        ArUco ●─────┼────────────● ArUco
              │     │            │
              │     │            │
              │   작업 영역        │
              │     │            │
              │     │            │
        ArUco ●─────┴────────────● ArUco
                    └──────────────→ X
                   (0,0)
```

---

## 3.3 ArUco Marker

ArUco Marker는 작업판의 네 모서리에 각각 하나씩 총 4개를 배치한다.

ArUco Marker는 폐기물의 종류를 인식하기 위한 것이 아니라 카메라 영상에서 작업판의 기준점을 찾고 좌표계를 구성하기 위한 용도로 사용한다.

각 Marker의 실제 작업판 좌표를 알고 있기 때문에 카메라 영상에서 검출된 Marker와 실제 작업판 좌표를 대응시킬 수 있다.

이를 통해 카메라 영상 좌표와 작업판 좌표 사이의 Homography 변환을 계산할 수 있다.

---

## 3.4 Robotis OpenManipulator-X

Robotis OpenManipulator-X는 인식된 폐기물을 실제로 집고 이동시키는 매니퓰레이터로 사용한다.

YOLOv8과 ArUco를 이용하여 계산된 폐기물의 위치를 Pick 목표 위치로 사용한다.

OpenManipulator-X는 ROS 2 및 MoveIt 2와 연동하여 목표 위치까지 이동한다.

---

## 3.5 Gripper

OpenManipulator-X의 말단에는 Gripper를 사용한다.

Gripper는 폐기물의 Pick Point로 이동하여 폐기물을 파지한다.

파지가 완료되면 로봇은 해당 폐기물을 지정된 분류 위치로 이동하고 Gripper를 개방하여 폐기물을 배치한다.

---

# 4. 소프트웨어 및 개발환경

| 구분 | 사용 기술 |
|---|---|
| Host OS | Windows |
| Linux 환경 | WSL2 |
| Linux 배포판 | Ubuntu |
| Robot Middleware | ROS 2 |
| Programming Language | Python |
| AI Model | YOLOv8 |
| Image Processing | OpenCV |
| Object Detection | YOLOv8 |
| Marker Detection | ArUco |
| Coordinate Transformation | Homography |
| Robot Motion Planning | MoveIt 2 |
| Robot | Robotis OpenManipulator-X |
| Camera | USB RGB Camera |

Windows 환경에서 WSL2를 이용하여 Ubuntu/Linux 환경을 구성하고, Linux 환경에서 ROS 2 기반의 로봇 제어 시스템을 구성한다.

Python은 영상 처리, 객체 인식 및 좌표 계산 프로그램을 구현하는 데 사용한다.

OpenCV는 카메라 영상 처리와 ArUco Marker 검출에 사용하며, YOLOv8은 폐기물 객체 탐지에 사용한다.

---

# 5. AI 객체 인식 시스템

## 5.1 YOLOv8 기반 객체 탐지

폐기물의 종류를 인식하기 위해 YOLOv8 기반 객체 탐지 모델을 사용한다.

본 프로젝트에서는 Roboflow Universe에 등록된 여러 객체 탐지 모델을 사용한다.

사용하는 모델은 다음과 같다.

| 객체 | 모델 ID | 이미지 수 | 역할 |
|---|---|---:|---|
| Paper | `crumpledpaper/1` | 약 2.8k | 구겨진 종이 인식 |
| PET | `plastic-bottles-ip5yb-uziag-hg1ll/1` | 약 1.2k | 플라스틱 병 인식 |
| Can | `can-a8pgu/2` | 약 3.3k | 캔 인식 |
| Recyclables | `recyclables-vs.-non-recyclables-qzeyp-aeu0v/1` | - | 재활용 가능 여부 판단 |

각 모델을 이용하여 카메라 영상에서 해당 폐기물을 탐지한다.

---

# 6. 사용 YOLO 모델

## 6.1 구겨진 종이 모델

구겨진 종이를 인식하기 위해 다음 모델을 사용한다.

```python
PAPER_MODEL_ID = "crumpledpaper/1"
```

해당 모델은 약 2,800장의 이미지 데이터를 기반으로 구성되어 있으며, 구겨진 종이 객체를 탐지하는 데 사용한다.

---

## 6.2 플라스틱 병 모델

플라스틱 병을 인식하기 위해 다음 모델을 사용한다.

```python
PET_MODEL_ID = "plastic-bottles-ip5yb-uziag-hg1ll/1"
```

해당 모델은 약 1,200장의 이미지 데이터를 기반으로 구성되어 있으며, 플라스틱 병 객체를 탐지하는 데 사용한다.

---

## 6.3 캔 모델

캔을 인식하기 위해 다음 모델을 사용한다.

```python
CAN_MODEL_ID = "can-a8pgu/2"
```

해당 모델은 약 3,300장의 이미지 데이터를 기반으로 구성되어 있으며, 캔 객체를 탐지하는 데 사용한다.

본 프로젝트에서는 캔의 상태를 구분하지 않는다.

따라서 다음과 같은 캔의 상태 분류 기능은 사용하지 않는다.

- Intact
- Slightly-crushed
- Moderately-crushed
- Crushed

즉, 캔인지 아닌지를 객체 단위로 판단하는 데 초점을 둔다.

---

## 6.4 재활용 가능 여부 모델

재활용 가능 여부를 판단하기 위해 다음 모델을 사용한다.

```python
MODEL_ID = "recyclables-vs.-non-recyclables-qzeyp-aeu0v/1"
```

해당 모델은 폐기물의 재활용 가능 여부를 판단하기 위한 모델로 사용한다.

---

# 7. 폐기물 객체 인식 과정

카메라 영상이 입력되면 YOLOv8 모델을 이용하여 폐기물을 탐지한다.

객체가 탐지되면 다음과 같은 정보가 생성된다.

```text
객체 종류
     ↓
Bounding Box
     ↓
Confidence
     ↓
객체 위치
```

예를 들어 YOLO가 종이를 검출하면 해당 종이를 둘러싸는 Bounding Box가 생성된다.

```text
        x1                     x2
        │                      │
        ↓                      ↓
        ┌──────────────────────┐
        │                      │
        │       Paper          │
        │                      │
        │                      │
        └──────────────────────┘
        ↑                      ↑
       y1                      y2
```

Bounding Box의 좌표를 이용하여 폐기물의 Pick Point를 계산한다.

---

# 8. Bottom Center Point 기반 Pick 위치 결정

## 8.1 Bottom Center Point

본 시스템에서는 Bounding Box의 중심점을 Pick Point로 사용하지 않는다.

폐기물이 작업판 위에 놓여 있기 때문에 로봇의 Gripper가 접근해야 하는 위치를 고려하여 Bounding Box의 하단 중앙점을 Pick Point로 사용한다.

Bounding Box의 좌표가 다음과 같다고 가정한다.

```text
(x1, y1)
(x2, y2)
```

이때 Bottom Center Point는 다음과 같이 계산한다.

```text
u_c = (x1 + x2) / 2

v_c = y2
```

따라서 최종 Pick Point는 다음과 같다.

```text
Pick Point = (u_c, v_c)
```

여기서 `(u_c, v_c)`는 카메라 영상의 픽셀 좌표이다.

---

## 8.2 Bottom Center Point의 의미

Bottom Center Point는 폐기물의 실제 물리적인 밑면 중심점을 직접 측정하는 것이 아니다.

YOLO가 생성한 Bounding Box의 하단 중앙점을 이용하여 폐기물이 작업판과 접촉하고 있는 영역에 가까운 위치를 Pick 목표점으로 설정하는 방식이다.

따라서 다음과 같은 과정으로 사용한다.

```text
YOLO Bounding Box
        ↓
Bounding Box 하단
        ↓
하단 중앙점 계산
        ↓
Bottom Center Point
        ↓
Pick Point
```

---

# 9. ArUco 기반 작업판 XY 좌표계

## 9.1 좌표계 구성

작업판의 네 모서리에 배치된 4개의 ArUco Marker를 이용하여 작업판의 좌표계를 구성한다.

예를 들어 작업판의 좌측 하단을 원점 `(0,0)`으로 설정하면 다음과 같은 좌표계를 구성할 수 있다.

```text
                 Y
                 ↑
                 │
        ●────────┼────────●
        │                   │
        │                   │
        │       작업판       │
        │                   │
        │                   │
        ●───────────────────●
       (0,0)                → X
```

작업판의 실제 크기를 알고 있다면 각 모서리의 실제 좌표를 정의할 수 있다.

예를 들어 작업판의 크기가 `W × H`라면 다음과 같이 설정할 수 있다.

```text
좌측 하단 = (0, 0)
우측 하단 = (W, 0)
우측 상단 = (W, H)
좌측 상단 = (0, H)
```

---

# 10. ArUco Marker의 역할

ArUco Marker는 다음과 같은 역할을 수행한다.

### 1. 작업판 위치 검출

카메라 영상에서 작업판이 어디에 위치하고 있는지 판단한다.

### 2. 작업판 방향 판단

작업판이 카메라 영상에서 어떤 방향으로 보이는지 판단한다.

### 3. 기준점 제공

Homography 변환을 위한 기준점을 제공한다.

### 4. 작업판 좌표계 구성

카메라 영상 좌표와 실제 작업판 좌표를 연결한다.

---

# 11. Homography 기반 좌표 변환

YOLO가 계산한 Bottom Center Point는 카메라 영상의 픽셀 좌표이다.

그러나 OpenManipulator-X가 폐기물을 집기 위해서는 작업판을 기준으로 한 실제 XY 좌표가 필요하다.

따라서 Homography를 이용하여 영상 좌표를 작업판 좌표로 변환한다.

전체적인 과정은 다음과 같다.

```text
카메라 영상
     │
     ↓
Bottom Center Point
     │
     ↓
Pixel 좌표
(u, v)
     │
     ↓
Homography
     │
     ↓
작업판 좌표
(X, Y)
```

---

# 12. Homography 변환 원리

작업판은 평면으로 구성되어 있기 때문에 카메라 영상의 2차원 좌표와 작업판의 2차원 좌표 사이의 관계를 Homography를 이용하여 계산할 수 있다.

ArUco Marker의 영상 좌표와 실제 작업판 좌표를 대응시키면 Homography 행렬을 계산할 수 있다.

개념적으로 다음과 같은 관계를 가진다.

```text
      [u]
      [v]
      [1]
       │
       │ Homography Matrix
       ↓
      [X']
      [Y']
      [W']
```

Homography 행렬을 `H`라고 하면 다음과 같이 표현할 수 있다.

```text
[X']       [u]
[Y'] = H × [v]
[W']       [1]
```

최종 작업판 좌표는 다음과 같이 정규화하여 계산한다.

```text
X = X' / W'

Y = Y' / W'
```

이를 통해 YOLO에서 얻은 영상 좌표를 작업판의 실제 XY 좌표로 변환한다.

---

# 13. 폐기물 위치 계산 과정

전체적인 폐기물 위치 계산 과정은 다음과 같다.

```text
                카메라 영상
                     ↓
        ┌────────────┴────────────┐
        ↓                         ↓
     ArUco                      YOLOv8
     검출                       객체 검출
        ↓                         ↓
  작업판 기준점               Bounding Box
        ↓                         ↓
  XY 좌표계 구성         Bottom Center Point
        │                         │
        └───────────┬─────────────┘
                    ↓
               Homography
                    ↓
             작업판 XY 좌표
                    ↓
             로봇 Pick 위치
```

---

# 14. 로봇 목표 좌표 결정

Homography를 통해 계산된 작업판의 `(X,Y)` 좌표를 OpenManipulator-X의 Pick 목표 위치로 사용한다.

예를 들어 YOLO가 다음과 같은 Bottom Center Point를 계산했다고 가정한다.

```text
Pixel Coordinate

(u, v)
=
(520, 410)
```

Homography 변환을 수행하면 다음과 같은 작업판 좌표를 얻을 수 있다.

```text
Work Area Coordinate

(X, Y)
=
(185 mm, 240 mm)
```

이 좌표를 로봇의 Pick 위치 계산에 사용한다.

---

# 15. Z축 위치 처리

본 시스템에서는 Depth Camera를 사용하지 않기 때문에 카메라를 이용하여 폐기물의 깊이나 높이를 직접 측정하지 않는다.

대신 폐기물이 동일한 작업판 위에 놓여 있다는 조건을 이용한다.

따라서 작업판의 높이를 기준으로 로봇의 Z축 위치를 설정한다.

로봇은 다음과 같은 방식으로 Pick 동작을 수행한다.

```text
          접근 위치
              │
              ↓
        ─────────────
          작업판
              │
              ↓
          Pick 위치
              │
         Gripper 닫힘
              │
              ↓
             Lift
```

작업판의 높이를 기준으로 다음과 같은 위치를 사전에 설정할 수 있다.

```text
Approach Z
    ↓
Pick Z
    ↓
Lift Z
```

---

# 16. ROS 2 시스템

ROS 2는 카메라 및 AI 객체 인식 시스템과 OpenManipulator-X를 연결하는 로봇 미들웨어로 사용한다.

전체적인 데이터 흐름은 다음과 같다.

```text
USB RGB Camera
      ↓
OpenCV
      ↓
YOLOv8 / ArUco
      ↓
좌표 계산
      ↓
ROS 2
      ↓
MoveIt 2
      ↓
OpenManipulator-X
```

ROS 2를 통해 객체 인식 시스템에서 계산한 로봇 목표 위치를 로봇 제어 시스템으로 전달한다.

---

# 17. MoveIt 2 기반 로봇 제어

MoveIt 2는 OpenManipulator-X의 움직임을 계획하기 위해 사용한다.

폐기물의 Pick 좌표가 결정되면 해당 좌표를 로봇의 목표 위치로 설정한다.

MoveIt 2는 로봇의 관절 상태와 작업 공간을 고려하여 목표 위치까지 이동하기 위한 경로를 계획한다.

전체적인 과정은 다음과 같다.

```text
폐기물 좌표
     ↓
Robot Target Position
     ↓
MoveIt 2
     ↓
Motion Planning
     ↓
OpenManipulator-X
```

---

# 18. Pick 동작

폐기물의 위치가 결정되면 OpenManipulator-X는 다음과 같은 순서로 Pick 동작을 수행한다.

```text
1. 폐기물 위치 확인
        ↓
2. Pick Point 계산
        ↓
3. Approach 위치로 이동
        ↓
4. Pick 위치로 하강
        ↓
5. Gripper 닫힘
        ↓
6. 폐기물 파지
        ↓
7. Lift
```

---

# 19. Place 동작

폐기물을 파지한 후 객체 종류에 따라 지정된 분류 위치로 이동한다.

```text
폐기물 파지
     ↓
분류 위치 결정
     ↓
MoveIt 2 경로 계획
     ↓
분류 위치로 이동
     ↓
Gripper 개방
     ↓
폐기물 배치
```

---

# 20. 객체별 자동 분류

YOLOv8이 인식한 객체 종류를 기준으로 각각의 분류 위치를 지정한다.

| 객체 | 분류 위치 |
|---|---|
| Crumpled Paper | 위치 A |
| Plastic Bottle | 위치 B |
| Can | 위치 C |
| Non-recyclable | 위치 D |

실제 분류 위치는 작업판의 구성 및 OpenManipulator-X의 작업 범위에 맞게 설정한다.

---

# 21. 전체 Pick & Place 알고리즘

전체적인 시스템 동작 알고리즘은 다음과 같다.

```text
① USB RGB Camera 영상 획득
        ↓
② ArUco Marker 검출
        ↓
③ 작업판 4개 기준점 확인
        ↓
④ 작업판 XY 좌표계 구성
        ↓
⑤ YOLOv8 폐기물 검출
        ↓
⑥ Bounding Box 생성
        ↓
⑦ Bottom Center Point 계산
        ↓
⑧ Pixel 좌표 획득
        ↓
⑨ Homography 좌표 변환
        ↓
⑩ 작업판 XY 좌표 계산
        ↓
⑪ Robot Pick 목표 위치 결정
        ↓
⑫ ROS 2로 좌표 전달
        ↓
⑬ MoveIt 2 경로 계획
        ↓
⑭ OpenManipulator-X 이동
        ↓
⑮ Gripper Pick
        ↓
⑯ Lift
        ↓
⑰ 폐기물 종류에 따른 분류 위치 결정
        ↓
⑱ 분류 위치로 이동
        ↓
⑲ Gripper Place
```

---

# 22. 전체 시스템 동작 원리

## Step 1. 카메라 영상 획득

USB RGB Camera를 이용하여 작업판 전체를 촬영한다.

---

## Step 2. ArUco Marker 검출

작업판 네 모서리에 배치된 4개의 ArUco Marker를 검출한다.

검출된 Marker를 통해 작업판의 네 모서리 위치를 확인한다.

---

## Step 3. 작업판 좌표계 구성

검출된 ArUco Marker와 작업판의 실제 좌표를 대응시켜 작업판의 XY 좌표계를 구성한다.

---

## Step 4. 폐기물 탐지

YOLOv8을 이용하여 카메라 영상에서 폐기물을 탐지한다.

폐기물의 종류에 따라 Paper, PET, Can 등의 객체를 검출한다.

---

## Step 5. Bounding Box 생성

YOLO가 폐기물을 탐지하면 해당 객체를 둘러싸는 Bounding Box를 생성한다.

---

## Step 6. Bottom Center Point 계산

Bounding Box의 하단 중앙점을 계산한다.

```text
u_c = (x1 + x2) / 2
v_c = y2
```

계산된 `(u_c, v_c)`를 Pick Point의 영상 좌표로 사용한다.

---

## Step 7. Homography 변환

Bottom Center Point의 픽셀 좌표를 Homography를 이용하여 작업판의 실제 XY 좌표로 변환한다.

---

## Step 8. 로봇 목표 위치 결정

변환된 `(X,Y)` 좌표를 OpenManipulator-X의 Pick 목표 위치로 사용한다.

---

## Step 9. ROS 2 전달

계산된 목표 좌표를 ROS 2를 통해 로봇 제어 시스템으로 전달한다.

---

## Step 10. MoveIt 2 경로 계획

MoveIt 2를 이용하여 OpenManipulator-X가 목표 Pick 위치까지 이동할 수 있는 경로를 계획한다.

---

## Step 11. 폐기물 Pick

OpenManipulator-X가 폐기물 위치로 이동한다.

Gripper를 닫아 폐기물을 파지한다.

---

## Step 12. 분류 위치 이동

YOLO가 판단한 폐기물의 종류에 따라 지정된 분류 위치를 결정한다.

OpenManipulator-X는 해당 위치로 이동한다.

---

## Step 13. 폐기물 Place

분류 위치에 도착하면 Gripper를 개방하여 폐기물을 내려놓는다.

---

# 23. 시스템 핵심 기술

## 23.1 RGB 카메라 기반 객체 인식

본 시스템은 별도의 Depth Camera를 사용하지 않고 USB RGB Camera 한 대를 이용하여 폐기물을 인식한다.

RGB 영상에서 ArUco Marker와 폐기물을 동시에 검출한다.

---

## 23.2 ArUco 기반 작업판 좌표계

작업판 네 모서리에 배치된 4개의 ArUco Marker를 이용하여 작업판의 기준점을 확보한다.

이를 통해 카메라 영상 좌표와 작업판 좌표 사이의 관계를 계산할 수 있다.

---

## 23.3 Bottom Center Point 기반 Pick 위치 결정

YOLO Bounding Box의 중심점 대신 하단 중앙점을 Pick Point로 사용한다.

이를 통해 작업판 위에 놓여 있는 폐기물에 대해 로봇의 Gripper가 접근할 목표 위치를 결정한다.

---

## 23.4 Homography 기반 좌표 변환

작업판을 하나의 평면으로 간주하고 ArUco Marker를 기준점으로 사용한다.

카메라 영상의 Pixel 좌표를 작업판의 실제 XY 좌표로 변환한다.

이를 통해 Depth Camera 없이도 폐기물의 작업판상 위치를 계산할 수 있다.

---

## 23.5 ROS 2 기반 시스템 통합

ROS 2를 이용하여 영상 처리 및 객체 인식 시스템과 OpenManipulator-X를 연결한다.

객체 인식 결과와 계산된 목표 좌표를 ROS 2를 통해 로봇 제어 시스템으로 전달한다.

---

## 23.6 MoveIt 2 기반 로봇 경로 계획

MoveIt 2를 이용하여 OpenManipulator-X가 폐기물 위치와 분류 위치 사이를 이동할 수 있도록 경로를 계획한다.

---

## 23.7 자동 Pick & Place

폐기물의 종류와 위치를 판단한 후 OpenManipulator-X가 자동으로 폐기물을 파지하고 지정된 위치에 배치한다.

---

# 24. 최종 시스템 구성도

```text
                         USB RGB Camera
                                │
                                ↓
                         OpenCV 영상 처리
                                │
                 ┌──────────────┴──────────────┐
                 │                             │
                 ↓                             ↓
           ArUco Detection                  YOLOv8
                 │                         Object Detection
                 ↓                             │
       4개 모서리 기준점 획득                  ↓
                 │                       Bounding Box
                 ↓                             │
          작업판 XY 좌표계 구성                ↓
                                           Bottom Center
                                              Point
                 │                             │
                 └──────────────┬──────────────┘
                                ↓
                       Homography 변환
                                ↓
                         작업판 X, Y 좌표
                                ↓
                        Robot Pick 좌표
                                ↓
                              ROS 2
                                ↓
                            MoveIt 2
                                ↓
                       OpenManipulator-X
                                ↓
                           Gripper Pick
                                ↓
                       분류 위치로 이동
                                ↓
                         Gripper Place
```

---

# 25. 최종 기술 요약

본 프로젝트는 USB RGB Camera와 YOLOv8, ArUco Marker, Homography 및 Robotis OpenManipulator-X를 이용한 폐기물 자동 분류 시스템이다.

본 시스템에서는 Depth Camera를 사용하지 않고 RGB 카메라 한 대를 이용하여 폐기물의 인식과 위치 계산을 수행한다.

작업판의 네 모서리에는 4개의 ArUco Marker를 배치한다.

ArUco Marker를 카메라로 검출하여 작업판의 기준점을 확보하고, 작업판을 기준으로 하는 XY 좌표계를 구성한다.

동일한 카메라 영상에서 YOLOv8을 이용하여 폐기물을 탐지한다.

YOLO가 객체를 탐지하면 Bounding Box가 생성되고, 해당 Bounding Box의 하단 중앙점을 Bottom Center Point로 계산한다.

```text
u_c = (x1 + x2) / 2
v_c = y2
```

계산된 Bottom Center Point는 카메라 영상의 Pixel 좌표이다.

이 Pixel 좌표를 ArUco Marker를 기준으로 계산된 Homography 변환을 이용하여 작업판의 XY 좌표로 변환한다.

```text
Pixel Coordinate
      ↓
(u, v)
      ↓
Homography
      ↓
(X, Y)
      ↓
Robot Pick Position
```

변환된 좌표는 ROS 2를 통해 로봇 제어 시스템으로 전달된다.

MoveIt 2는 해당 좌표를 기준으로 OpenManipulator-X의 이동 경로를 계획한다.

OpenManipulator-X는 폐기물의 Pick Point로 이동하여 Gripper로 폐기물을 파지하고, 폐기물의 종류에 따라 지정된 분류 위치로 이동한다.

이후 Gripper를 개방하여 폐기물을 해당 위치에 배치한다.

최종적인 시스템 처리 과정은 다음과 같다.

```text
ArUco Marker 검출
        ↓
작업판 XY 좌표계 구성
        ↓
YOLOv8 폐기물 탐지
        ↓
Bounding Box 생성
        ↓
Bottom Center Point 계산
        ↓
Homography 좌표 변환
        ↓
작업판 XY 좌표 계산
        ↓
ROS 2
        ↓
MoveIt 2
        ↓
OpenManipulator-X
        ↓
Gripper Pick
        ↓
분류 위치 이동
        ↓
Gripper Place
```

본 시스템의 핵심은 RGB 카메라 영상에서 **ArUco Marker를 이용하여 작업판의 좌표계를 구성하고, YOLOv8이 검출한 폐기물의 Bottom Center Point를 Homography를 통해 작업판의 XY 좌표로 변환하는 것**이다.

이를 통해 별도의 Depth Camera 없이도 작업판 위에 놓인 폐기물의 위치를 계산하고, 계산된 위치를 OpenManipulator-X의 자동 Pick & Place 동작에 활용할 수 있다.
