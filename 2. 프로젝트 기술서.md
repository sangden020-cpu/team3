# 프로젝트 기술서

# RGB 카메라와 ArUco 기반 폐기물 자동 분류 및 Pick & Place 시스템

---

# 1. 프로젝트 기본 정보

| 항목 | 내용 |
|---|---|
| 프로젝트명 | RGB 카메라와 ArUco 기반 폐기물 자동 분류 및 Pick & Place 시스템 |
| 프로젝트 기간 | 2026년 8월 10일 ~ 2026년 8월 14일 |
| 팀원 1 | 오우진 |
| 팀원 2 | 윤형식 |
| 팀원 3 | 장혜원 |
| 팀원 4 | 김헌주 |
| 개발 환경 | Windows + WSL2 + Ubuntu + ROS 2 |
| 프로그래밍 언어 | Python |
| 사용 로봇 | Robotis OpenManipulator-X |
| 사용 카메라 | USB RGB Camera |
| 객체 인식 모델 | YOLOv8 |
| 영상 처리 | OpenCV |
| 기준점 검출 | ArUco Marker |
| 좌표 변환 | Homography |
| 로봇 제어 | ROS 2 |

---

# 2. 프로젝트 배경 및 필요성

재활용 가능한 폐기물을 종류에 따라 분류하는 작업은 반복적인 작업이 많으며, 작업자가 지속적으로 폐기물의 종류와 위치를 판단하고 직접 이동시켜야 한다.

이러한 작업을 자동화하기 위해서는 카메라를 이용하여 폐기물의 종류와 위치를 인식하고, 인식된 결과를 바탕으로 로봇 매니퓰레이터가 폐기물을 직접 집어 지정된 위치에 분류하는 시스템이 필요하다.

본 프로젝트에서는 RGB 카메라와 객체 인식 AI를 이용하여 작업판 위에 놓인 폐기물을 인식하고, Robotis OpenManipulator-X를 이용하여 폐기물을 자동으로 집어 지정된 위치에 분류하는 시스템을 구축한다.

특히 본 프로젝트에서는 단순히 폐기물의 종류만 인식하는 것에서 끝나지 않고, 인식된 폐기물의 영상 좌표를 실제 작업판의 XY 좌표로 변환하여 로봇이 해당 위치로 이동할 수 있도록 한다.

이를 위해 작업판의 네 모서리에 ArUco Marker를 배치하고, 이를 기준으로 작업판의 XY 좌표계를 구성한다.

YOLOv8이 폐기물을 탐지하면 Bounding Box의 하단 중앙점(Bottom Center Point)을 Pick Point로 설정하고, 해당 픽셀 좌표를 Homography를 이용하여 작업판의 XY 좌표로 변환한다.

변환된 좌표는 ROS 2를 통해 OpenManipulator-X의 목표 위치로 전달되며, 로봇은 해당 위치에서 폐기물을 집은 후 폐기물의 종류에 따라 지정된 위치로 이동하여 분류한다.

---

# 3. 프로젝트 목적

본 프로젝트의 목적은 RGB 카메라 기반 객체 인식 시스템과 Robotis OpenManipulator-X를 결합하여 폐기물의 자동 분류 및 Pick & Place 시스템을 구현하는 것이다.

이를 위해 다음과 같은 기능을 구현하는 것을 목적으로 한다.

1. RGB 카메라를 이용한 작업판 영상 획득
2. ArUco Marker를 이용한 작업판 기준점 검출
3. 작업판을 기준으로 하는 XY 좌표계 구성
4. YOLOv8을 이용한 폐기물 객체 탐지
5. 객체의 Bounding Box 하단 중앙점(Bottom Center Point) 계산
6. Homography를 이용한 영상 좌표와 작업판 좌표 간 변환
7. 폐기물 위치를 로봇 Pick 좌표로 변환
8. ROS 2를 이용한 객체 인식 시스템과 로봇 시스템 연동
9. ROS 2를 이용한 OpenManipulator-X 제어
10. OpenManipulator-X를 이용한 폐기물 Pick & Place
11. 폐기물 종류에 따른 자동 분류

본 프로젝트의 핵심은 다음과 같은 영상 인식 및 로봇 제어 과정을 하나의 시스템으로 통합하는 것이다.

```text
YOLOv8 객체 인식
       ↓
Bottom Center Point
       ↓
ArUco 기반 좌표계
       ↓
Homography 좌표 변환
       ↓
작업판 XY 좌표
       ↓
     ROS 2
       ↓
OpenManipulator-X
       ↓
Pick & Place
```

---

# 4. 프로젝트 범위

본 프로젝트에서는 작업판 위에 배치된 폐기물을 대상으로 RGB 카메라 기반 객체 인식 및 로봇 Pick & Place를 수행한다.

객체 인식에는 YOLOv8 기반 모델을 사용하며, 구겨진 종이, 플라스틱 병, 캔 등의 폐기물을 대상으로 한다.

폐기물의 위치는 별도의 Depth Camera를 이용하지 않고, 작업판 네 모서리에 배치된 4개의 ArUco Marker를 기준으로 계산한다.

YOLOv8이 검출한 폐기물의 Bounding Box에서 Bottom Center Point를 계산하고, 해당 픽셀 좌표를 Homography를 이용하여 작업판의 XY 좌표로 변환한다.

변환된 좌표는 OpenManipulator-X의 Pick 목표 위치로 사용되며, ROS 2를 통해 로봇을 제어한다.

본 프로젝트의 주요 범위는 다음과 같다.

- RGB 카메라 기반 영상 획득
- RGB 영상 기반 폐기물 객체 탐지
- ArUco Marker 기반 작업판 기준점 검출
- 작업판 XY 좌표계 구성
- YOLO Bounding Box 기반 Bottom Center Point 계산
- Homography 기반 좌표 변환
- 폐기물 Pick 위치 계산
- ROS 2 기반 시스템 연동
- ROS 2 기반 OpenManipulator-X 제어
- OpenManipulator-X Pick & Place
- 폐기물 종류별 자동 분류

다음 기능은 본 프로젝트의 범위에서 제외한다.

- Depth Camera를 이용한 거리 측정
- Depth Camera를 이용한 3차원 객체 위치 측정
- 캔의 압착 정도 및 상태 측정
- 캔의 상태별 세부 분류
- 재활용 가능 여부 별도 판별

---

# 5. 시스템 개요

본 시스템은 크게 다음과 같은 구성으로 이루어진다.

1. 영상 획득 및 처리부
2. ArUco Marker 기반 좌표계 구성부
3. YOLOv8 객체 인식부
4. 좌표 변환부
5. ROS 2 통신 및 로봇 제어부
6. OpenManipulator-X Pick & Place부

USB RGB Camera를 통해 작업판 전체를 촬영하고 OpenCV를 이용하여 영상을 처리한다.

카메라 영상에서 작업판 네 모서리의 ArUco Marker를 검출하여 작업판의 기준점을 확보하고 XY 좌표계를 구성한다.

동시에 YOLOv8을 이용하여 작업판 위에 놓인 폐기물을 탐지한다.

탐지된 객체의 Bounding Box 하단 중앙점을 Bottom Center Point로 계산하고, 해당 픽셀 좌표를 Homography를 이용하여 작업판의 XY 좌표로 변환한다.

계산된 좌표는 ROS 2를 통해 로봇 제어 시스템으로 전달된다.

ROS 2를 이용하여 OpenManipulator-X의 목표 위치와 동작을 제어한다.

로봇은 폐기물의 Pick 위치로 이동하여 Gripper로 폐기물을 집고, 폐기물의 종류에 따라 지정된 분류 위치로 이동한 후 폐기물을 배치한다.

---

# 6. 시스템 전체 구성도

```text
                         USB RGB Camera
                                │
                                ↓
                         OpenCV 영상 처리
                                │
                 ┌──────────────┴──────────────┐
                 │                             │
                 ↓                             ↓
           ArUco Marker                    YOLOv8
              검출                       객체 탐지
                 │                             │
                 ↓                             ↓
       4개 모서리 기준점                   폐기물 검출
                 │                             │
                 ↓                             ↓
        작업판 XY 좌표계                 Bounding Box
             구성                            │
                                               ↓
                                      Bottom Center Point
                                               │
                 └──────────────┬──────────────┘
                                ↓
                       Homography 변환
                                ↓
                         작업판 XY 좌표
                                ↓
                       로봇 Pick 목표 위치
                                ↓
                              ROS 2
                                ↓
                       OpenManipulator-X
                                ↓
                           Gripper Pick
                                ↓
                       분류 위치로 이동
                                ↓
                         Gripper Place
```

---

# 7. 하드웨어 구성

## 7.1 USB RGB Camera

본 시스템의 영상 입력 장치로 USB RGB Camera를 사용한다.

USB RGB Camera는 작업판과 그 위에 배치된 폐기물을 촬영하며, 획득한 RGB 영상을 OpenCV를 통해 처리한다.

하나의 카메라 영상에서 다음 두 가지 작업을 수행한다.

1. ArUco Marker 검출
2. YOLOv8 기반 폐기물 객체 검출

본 시스템에서는 Depth Camera를 사용하지 않으며, RGB 영상만을 이용하여 폐기물의 인식과 위치 계산을 수행한다.

카메라는 다음과 같은 정보를 제공한다.

- 작업판 영상
- ArUco Marker의 영상 좌표
- 폐기물의 Bounding Box
- 폐기물 Pick Point의 픽셀 좌표

---

## 7.2 작업판

폐기물이 놓이는 평면 형태의 작업판을 구성한다.

작업판은 폐기물의 배치 영역이면서 동시에 로봇의 Pick 작업이 이루어지는 영역이다.

작업판의 네 모서리에 각각 하나씩 총 4개의 ArUco Marker를 배치하여 작업판의 기준점을 구성한다.

작업판의 한 모서리를 원점으로 설정하고 작업판의 가로 방향과 세로 방향을 각각 X축과 Y축으로 설정한다.

```text
                        x
                        ↑
                        │
        ArUco ●─────────┼──────────● ArUco
              │         │          │
              │         │          │
              │     작업 영역      │
              │         │          │
              │         │          │
        ArUco ●─────────┴──────────● ArUco
               y←─────
                    (0,0)
```

※ 실제 시스템에서는 로봇의 좌표계 및 작업판 설정에 맞춰 X축과 Y축 방향을 정의하며, 본 프로젝트에서는 기존에 반대로 적용되어 있던 X/Y 좌표 방향을 실제 좌표계에 맞게 수정하여 사용한다.

---

## 7.3 ArUco Marker

ArUco Marker는 작업판의 네 모서리에 각각 하나씩 총 4개를 배치한다.

ArUco Marker는 폐기물의 종류를 인식하기 위한 것이 아니라 카메라 영상에서 작업판의 기준점을 찾고 좌표계를 구성하기 위한 용도로 사용한다.

각 Marker의 실제 작업판 좌표를 알고 있기 때문에 카메라 영상에서 검출된 Marker와 실제 작업판 좌표를 대응시킬 수 있다.

이를 통해 카메라 영상 좌표와 작업판 좌표 사이의 Homography 변환을 계산할 수 있다.

---

## 7.4 Robotis OpenManipulator-X

Robotis OpenManipulator-X는 인식된 폐기물을 실제로 집고 이동시키는 매니퓰레이터로 사용한다.

YOLOv8과 ArUco를 이용하여 계산된 폐기물의 위치를 Pick 목표 위치로 사용한다.

OpenManipulator-X는 ROS 2를 통해 직접 제어하며, 계산된 목표 좌표를 이용하여 폐기물 위치와 분류 위치 사이를 이동한다.

---

## 7.5 Gripper

OpenManipulator-X의 말단에는 Gripper를 사용한다.

Gripper는 폐기물의 Pick Point로 이동하여 폐기물을 파지한다.

파지가 완료되면 로봇은 해당 폐기물을 지정된 분류 위치로 이동하고 Gripper를 개방하여 폐기물을 배치한다.

---

# 8. 소프트웨어 및 개발환경

| 구분 | 사용 기술 |
|---|---|
| Host OS | Windows |
| Linux 환경 | WSL2 |
| Linux 배포판 | Ubuntu |
| Robot Middleware | ROS 2 |
| Programming Language | Python |
| AI Model | YOLOv8 |
| Image Processing | OpenCV |
| Object Detection | YOLOv8 |
| Marker Detection | ArUco |
| Coordinate Transformation | Homography |
| Robot Control | ROS 2 |
| Robot | Robotis OpenManipulator-X |
| Camera | USB RGB Camera |

Windows 환경에서 WSL2를 이용하여 Ubuntu/Linux 환경을 구성하고, Linux 환경에서 ROS 2 기반의 로봇 제어 시스템을 구성한다.

Python은 영상 처리, 객체 인식 및 좌표 계산 프로그램을 구현하는 데 사용한다.

OpenCV는 카메라 영상 처리와 ArUco Marker 검출에 사용하며, YOLOv8은 폐기물 객체 탐지에 사용한다.

ROS 2는 객체 인식 시스템과 OpenManipulator-X를 연결하고 로봇의 동작을 제어하는 데 사용한다.

---

# 9. AI 객체 인식 시스템

## 9.1 YOLOv8 기반 객체 탐지

폐기물의 종류를 인식하기 위해 YOLOv8 기반 객체 탐지 모델을 사용한다.

본 프로젝트에서는 Roboflow Universe에 등록된 객체 탐지 모델을 사용한다.

최종적으로 사용하는 모델은 다음과 같다.

| 객체 | 모델 ID | 이미지 수 | 역할 |
|---|---|---:|---|
| Paper | `crumpledpaper/1` | 약 2.8k | 구겨진 종이 인식 |
| PET | `plastic-bottles-ip5yb-uziag-hg1ll/1` | 약 1.2k | 플라스틱 병 인식 |
| Can | `can-a8pgu/2` | 약 3.3k | 캔 인식 |

각 모델을 이용하여 카메라 영상에서 해당 폐기물을 탐지한다.

---

# 10. 사용 YOLO 모델

## 10.1 구겨진 종이 모델

구겨진 종이를 인식하기 위해 다음 모델을 사용한다.

```python
PAPER_MODEL_ID = "crumpledpaper/1"
```

해당 모델은 약 2,800장의 이미지 데이터를 기반으로 구성되어 있으며, 구겨진 종이 객체를 탐지하는 데 사용한다.

---

## 10.2 플라스틱 병 모델

플라스틱 병을 인식하기 위해 다음 모델을 사용한다.

```python
PET_MODEL_ID = "plastic-bottles-ip5yb-uziag-hg1ll/1"
```

해당 모델은 약 1,200장의 이미지 데이터를 기반으로 구성되어 있으며, 플라스틱 병 객체를 탐지하는 데 사용한다.

---

## 10.3 캔 모델

캔을 인식하기 위해 다음 모델을 사용한다.

```python
CAN_MODEL_ID = "can-a8pgu/2"
```

해당 모델은 약 3,300장의 이미지 데이터를 기반으로 구성되어 있으며, 캔 객체를 탐지하는 데 사용한다.

본 프로젝트에서는 캔의 압착 정도나 상태를 별도로 측정하지 않는다.

따라서 다음과 같은 캔 상태 분류 기능은 사용하지 않는다.

- Intact
- Slightly-crushed
- Moderately-crushed
- Crushed

본 시스템에서는 캔 객체를 탐지하고 해당 객체의 위치를 계산하는 데 초점을 둔다.

---

# 11. 폐기물 객체 인식 과정

카메라 영상이 입력되면 YOLOv8 모델을 이용하여 폐기물을 탐지한다.

객체가 탐지되면 다음과 같은 정보가 생성된다.

```text
객체 종류
     ↓
Bounding Box
     ↓
Confidence
     ↓
객체 위치
```

예를 들어 YOLO가 종이를 검출하면 해당 종이를 둘러싸는 Bounding Box가 생성된다.

```text
        x1                     x2
        │                      │
        ↓                      ↓
        ┌──────────────────────┐
        │                      │
        │       Paper          │
        │                      │
        │                      │
        └──────────────────────┘
        ↑                      ↑
       y1                      y2
```

Bounding Box의 좌표를 이용하여 폐기물의 Pick Point를 계산한다.

---

# 12. Bottom Center Point 기반 Pick 위치 결정

## 12.1 Bottom Center Point

본 시스템에서는 Bounding Box의 중심점을 Pick Point로 사용하지 않는다.

폐기물이 작업판 위에 놓여 있기 때문에 로봇의 Gripper가 접근해야 하는 위치를 고려하여 Bounding Box의 하단 중앙점을 Pick Point로 사용한다.

Bounding Box의 좌표가 다음과 같다고 가정한다.

```text
(x1, y1)
(x2, y2)
```

이때 Bottom Center Point는 다음과 같이 계산한다.

```text
u_c = (x1 + x2) / 2

v_c = y2
```

따라서 최종 Pick Point는 다음과 같다.

```text
Pick Point = (u_c, v_c)
```

여기서 `(u_c, v_c)`는 카메라 영상의 픽셀 좌표이다.

---

## 12.2 Bottom Center Point의 의미

Bottom Center Point는 폐기물의 실제 물리적인 밑면 중심점을 직접 측정하는 것이 아니다.

YOLO가 생성한 Bounding Box의 하단 중앙점을 이용하여 폐기물이 작업판과 접촉하고 있는 영역에 가까운 위치를 Pick 목표점으로 설정하는 방식이다.

따라서 다음과 같은 과정으로 사용한다.

```text
YOLO Bounding Box
        ↓
Bounding Box 하단
        ↓
하단 중앙점 계산
        ↓
Bottom Center Point
        ↓
Pick Point
```

---

# 13. ArUco 기반 작업판 XY 좌표계

## 13.1 좌표계 구성

작업판의 네 모서리에 배치된 4개의 ArUco Marker를 이용하여 작업판의 좌표계를 구성한다.

작업판의 한 모서리를 원점 `(0,0)`으로 설정하고, 실제 작업판의 방향과 로봇 좌표계를 기준으로 X축과 Y축을 정의한다.

```text
                 Y
                 ↑
                 │
        ●────────┼────────●
        │                   │
        │                   │
        │       작업판       │
        │                   │
        │                   │
        ●───────────────────●
       y ←---  (0,0)                
```

작업판의 실제 크기를 알고 있다면 각 모서리의 실제 좌표를 정의할 수 있다.

예를 들어 작업판의 크기가 `W × H`이고 위의 좌표계 기준으로 설정한다면 다음과 같이 정의할 수 있다.

```text
좌측 하단 = (0, 0)
우측 하단 = (W, 0)
우측 상단 = (W, H)
좌측 상단 = (0, H)
```

실제 로봇 제어에서는 이 작업판 좌표계와 로봇 좌표계의 방향을 일치시키도록 설정한다.

---

# 14. ArUco Marker의 역할

ArUco Marker는 다음과 같은 역할을 수행한다.

### 1. 작업판 위치 검출

카메라 영상에서 작업판이 어디에 위치하고 있는지 판단한다.

### 2. 작업판 방향 판단

작업판이 카메라 영상에서 어떤 방향으로 보이는지 판단한다.

### 3. 기준점 제공

Homography 변환을 위한 기준점을 제공한다.

### 4. 작업판 좌표계 구성

카메라 영상 좌표와 실제 작업판 좌표를 연결한다.

---

# 15. Homography 기반 좌표 변환

YOLO가 계산한 Bottom Center Point는 카메라 영상의 픽셀 좌표이다.

그러나 OpenManipulator-X가 폐기물을 집기 위해서는 작업판을 기준으로 한 실제 XY 좌표가 필요하다.

따라서 Homography를 이용하여 영상 좌표를 작업판 좌표로 변환한다.

전체적인 과정은 다음과 같다.

```text
카메라 영상
     │
     ↓
Bottom Center Point
     │
     ↓
Pixel 좌표
(u, v)
     │
     ↓
Homography
     │
     ↓
작업판 좌표
(X, Y)
```

---

# 16. Homography 변환 원리

작업판은 평면으로 구성되어 있기 때문에 카메라 영상의 2차원 좌표와 작업판의 2차원 좌표 사이의 관계를 Homography를 이용하여 계산할 수 있다.

ArUco Marker의 영상 좌표와 실제 작업판 좌표를 대응시키면 Homography 행렬을 계산할 수 있다.

개념적으로 다음과 같은 관계를 가진다.

```text
      [u]
      [v]
      [1]
       │
       │ Homography Matrix
       ↓
      [X']
      [Y']
      [W']
```

Homography 행렬을 `H`라고 하면 다음과 같이 표현할 수 있다.

```text
[X']       [u]
[Y'] = H × [v]
[W']       [1]
```

최종 작업판 좌표는 다음과 같이 정규화하여 계산한다.

```text
X = X' / W'

Y = Y' / W'
```

이를 통해 YOLO에서 얻은 영상 좌표를 작업판의 실제 XY 좌표로 변환한다.

---

# 17. 폐기물 위치 계산 과정

전체적인 폐기물 위치 계산 과정은 다음과 같다.

```text
                카메라 영상
                     ↓
        ┌────────────┴────────────┐
        ↓                         ↓
     ArUco                      YOLOv8
     검출                       객체 검출
        ↓                         ↓
  작업판 기준점               Bounding Box
        ↓                         ↓
  XY 좌표계 구성         Bottom Center Point
        │                         │
        └───────────┬─────────────┘
                    ↓
               Homography
                    ↓
             작업판 XY 좌표
                    ↓
             로봇 Pick 위치
```

---

# 18. 로봇 목표 좌표 결정

Homography를 통해 계산된 작업판의 `(X,Y)` 좌표를 OpenManipulator-X의 Pick 목표 위치로 사용한다.

작업판의 XY 좌표와 로봇의 좌표계 방향을 일치시킨 상태에서 계산된 좌표를 로봇의 목표 위치로 전달한다.

예를 들어 YOLO가 특정 폐기물의 Bottom Center Point를 계산하고 Homography 변환을 수행하면 다음과 같은 작업판 좌표를 얻을 수 있다.

```text
Work Area Coordinate

(X, Y)
=
(185 mm, 240 mm)
```

이 좌표를 로봇의 Pick 위치 계산에 사용한다.

※ 기존 기술서에서는 X와 Y의 방향이 서로 반대로 기재되어 있었으므로, 실제 시스템의 좌표계에 맞게 X축과 Y축의 방향을 수정하여 적용한다.

---

# 19. Z축 위치 처리

본 시스템에서는 Depth Camera를 사용하지 않기 때문에 카메라를 이용하여 폐기물의 깊이나 높이를 직접 측정하지 않는다.

대신 폐기물이 동일한 작업판 위에 놓여 있다는 조건을 이용한다.

따라서 작업판의 높이를 기준으로 로봇의 Z축 위치를 설정한다.

로봇은 다음과 같은 방식으로 Pick 동작을 수행한다.

```text
          접근 위치
              │
              ↓
        ─────────────
          작업판
              │
              ↓
          Pick 위치
              │
         Gripper 닫힘
              │
              ↓
             Lift
```

작업판의 높이를 기준으로 다음과 같은 위치를 사전에 설정할 수 있다.

```text
Approach Z
    ↓
Pick Z
    ↓
Lift Z
```

---

# 20. ROS 2 시스템

ROS 2는 카메라 및 AI 객체 인식 시스템과 OpenManipulator-X를 연결하는 로봇 미들웨어로 사용한다.

전체적인 데이터 흐름은 다음과 같다.

```text
USB RGB Camera
      ↓
OpenCV
      ↓
YOLOv8 / ArUco
      ↓
좌표 계산
      ↓
ROS 2
      ↓
OpenManipulator-X
```

ROS 2를 통해 객체 인식 시스템에서 계산한 목표 위치를 로봇 제어 시스템으로 전달한다.

또한 ROS 2 기반의 로봇 제어 명령을 통해 OpenManipulator-X의 관절 및 Gripper 동작을 수행한다.

---

# 21. ROS 2 기반 로봇 제어

OpenManipulator-X는 ROS 2를 이용하여 직접 제어한다.

객체의 Pick 좌표가 결정되면 해당 좌표를 기준으로 로봇의 관절 목표값 또는 로봇 동작 명령을 설정한다.

ROS 2를 통해 OpenManipulator-X에 목표 동작을 전달하고, 로봇은 지정된 Pick 위치로 이동한다.

전체적인 과정은 다음과 같다.

```text
폐기물 좌표
     ↓
Robot Target Position
     ↓
ROS 2
     ↓
OpenManipulator-X
     ↓
로봇 이동
```

---

# 22. Pick 동작

폐기물의 위치가 결정되면 OpenManipulator-X는 다음과 같은 순서로 Pick 동작을 수행한다.

```text
1. 폐기물 위치 확인
        ↓
2. Pick Point 계산
        ↓
3. 접근 위치로 이동
        ↓
4. Pick 위치로 하강
        ↓
5. Gripper 닫힘
        ↓
6. 폐기물 파지
        ↓
7. Lift
```

---

# 23. Place 동작

폐기물을 파지한 후 객체 종류에 따라 지정된 분류 위치로 이동한다.

```text
폐기물 파지
     ↓
분류 위치 결정
     ↓
ROS 2 로봇 제어
     ↓
분류 위치로 이동
     ↓
Gripper 개방
     ↓
폐기물 배치
```

---

# 24. 객체별 자동 분류

YOLOv8이 인식한 객체 종류를 기준으로 각각의 분류 위치를 지정한다.

| 객체 | 분류 위치 |
|---|---|
| Crumpled Paper | 위치 A |
| Plastic Bottle | 위치 B |
| Can | 위치 C |

실제 분류 위치는 작업판의 구성 및 OpenManipulator-X의 작업 범위에 맞게 설정한다.

---

# 25. 전체 Pick & Place 알고리즘

전체적인 시스템 동작 알고리즘은 다음과 같다.

```text
① USB RGB Camera 영상 획득
        ↓
② ArUco Marker 검출
        ↓
③ 작업판 4개 기준점 확인
        ↓
④ 작업판 XY 좌표계 구성
        ↓
⑤ YOLOv8 폐기물 검출
        ↓
⑥ Bounding Box 생성
        ↓
⑦ Bottom Center Point 계산
        ↓
⑧ Pixel 좌표 획득
        ↓
⑨ Homography 좌표 변환
        ↓
⑩ 작업판 XY 좌표 계산
        ↓
⑪ Robot Pick 목표 위치 결정
        ↓
⑫ ROS 2로 좌표 전달
        ↓
⑬ OpenManipulator-X 이동
        ↓
⑭ Gripper Pick
        ↓
⑮ Lift
        ↓
⑯ 폐기물 종류에 따른 분류 위치 결정
        ↓
⑰ 분류 위치로 이동
        ↓
⑱ Gripper Place
```

---

# 26. 시스템 동작 원리

## Step 1. 카메라 영상 획득

USB RGB Camera를 이용하여 작업판 전체를 촬영한다.

---

## Step 2. ArUco Marker 검출

작업판 네 모서리에 배치된 4개의 ArUco Marker를 검출한다.

검출된 Marker를 통해 작업판의 네 모서리 위치를 확인한다.

---

## Step 3. 작업판 좌표계 구성

검출된 ArUco Marker와 작업판의 실제 좌표를 대응시켜 작업판의 XY 좌표계를 구성한다.

X축과 Y축은 실제 로봇의 좌표계와 일치하도록 설정한다.

---

## Step 4. 폐기물 탐지

YOLOv8을 이용하여 카메라 영상에서 폐기물을 탐지한다.

폐기물의 종류에 따라 Paper, PET, Can 등의 객체를 검출한다.

---

## Step 5. Bounding Box 생성

YOLO가 폐기물을 탐지하면 해당 객체를 둘러싸는 Bounding Box를 생성한다.

---

## Step 6. Bottom Center Point 계산

Bounding Box의 하단 중앙점을 계산한다.

```text
u_c = (x1 + x2) / 2

v_c = y2
```

계산된 `(u_c, v_c)`를 Pick Point의 영상 좌표로 사용한다.

---

## Step 7. Homography 변환

Bottom Center Point의 픽셀 좌표를 Homography를 이용하여 작업판의 실제 XY 좌표로 변환한다.

---

## Step 8. 로봇 목표 위치 결정

변환된 `(X,Y)` 좌표를 OpenManipulator-X의 Pick 목표 위치로 사용한다.

---

## Step 9. ROS 2 전달

계산된 목표 좌표를 ROS 2를 통해 로봇 제어 시스템으로 전달한다.

---

## Step 10. OpenManipulator-X 이동

ROS 2를 이용하여 OpenManipulator-X를 계산된 Pick 위치로 이동시킨다.

---

## Step 11. 폐기물 Pick

OpenManipulator-X가 폐기물 위치로 이동한다.

Gripper를 닫아 폐기물을 파지한다.

---

## Step 12. 분류 위치 이동

YOLO가 판단한 폐기물의 종류에 따라 지정된 분류 위치를 결정한다.

OpenManipulator-X는 해당 위치로 이동한다.

---

## Step 13. 폐기물 Place

분류 위치에 도착하면 Gripper를 개방하여 폐기물을 내려놓는다.

---

# 27. 시스템 핵심 기술

## 27.1 RGB 카메라 기반 객체 인식

본 시스템은 별도의 Depth Camera를 사용하지 않고 USB RGB Camera 한 대를 이용하여 폐기물을 인식한다.

RGB 영상에서 ArUco Marker와 폐기물을 동시에 검출한다.

---

## 27.2 ArUco 기반 작업판 좌표계

작업판 네 모서리에 배치된 4개의 ArUco Marker를 이용하여 작업판의 기준점을 확보한다.

이를 통해 카메라 영상 좌표와 작업판 좌표 사이의 관계를 계산할 수 있다.

---

## 27.3 Bottom Center Point 기반 Pick 위치 결정

YOLO Bounding Box의 중심점 대신 하단 중앙점을 Pick Point로 사용한다.

이를 통해 작업판 위에 놓여 있는 폐기물에 대해 로봇의 Gripper가 접근할 목표 위치를 결정한다.

---

## 27.4 Homography 기반 좌표 변환

작업판을 하나의 평면으로 간주하고 ArUco Marker를 기준점으로 사용한다.

카메라 영상의 Pixel 좌표를 작업판의 실제 XY 좌표로 변환한다.

이를 통해 Depth Camera 없이도 작업판 위에 놓인 폐기물의 위치를 계산할 수 있다.

---

## 27.5 ROS 2 기반 시스템 통합

ROS 2를 이용하여 영상 처리 및 객체 인식 시스템과 OpenManipulator-X를 연결한다.

객체 인식 결과와 계산된 목표 좌표를 ROS 2를 통해 로봇 제어 시스템으로 전달한다.

---

## 27.6 ROS 2 기반 로봇 제어

본 시스템에서는 별도의 MoveIt 2를 사용하지 않고 ROS 2를 이용하여 OpenManipulator-X를 직접 제어한다.

객체의 Pick 위치와 분류 위치를 바탕으로 로봇의 이동 및 Gripper 동작을 수행한다.

---

## 27.7 자동 Pick & Place

폐기물의 종류와 위치를 판단한 후 OpenManipulator-X가 자동으로 폐기물을 파지하고 지정된 위치에 배치한다.

---

# 28. 전체 시스템 처리 흐름

본 시스템의 전체 처리 흐름은 다음과 같다.

```text
                    ┌─────────────────┐
                    │ USB RGB Camera  │
                    └────────┬────────┘
                             ↓
                    ┌─────────────────┐
                    │ OpenCV 영상처리 │
                    └────────┬────────┘
                             ↓
              ┌──────────────┴──────────────┐
              │                             │
              ↓                             ↓
     ┌─────────────────┐           ┌─────────────────┐
     │ ArUco Marker    │           │     YOLOv8      │
     │     검출        │           │  객체 탐지      │
     └────────┬────────┘           └────────┬────────┘
              ↓                             ↓
     작업판 기준점 획득               폐기물 검출
              ↓                             ↓
     XY 좌표계 구성               Bounding Box
              │                             ↓
              │                    Bottom Center Point
              │                             │
              └──────────────┬──────────────┘
                             ↓
                    ┌─────────────────┐
                    │   Homography    │
                    │   좌표 변환     │
                    └────────┬────────┘
                             ↓
                    작업판 X, Y 좌표
                             ↓
                    로봇 Pick 좌표
                             ↓
                    ┌─────────────────┐
                    │      ROS 2      │
                    └────────┬────────┘
                             ↓
                  ┌─────────────────────┐
                  │ OpenManipulator-X   │
                  └──────────┬──────────┘
                             ↓
                       Gripper Pick
                             ↓
                       Lift 이동
                             ↓
                      분류 위치 이동
                             ↓
                      Gripper Place
```

---

# 29. 시스템의 주요 특징

## 29.1 Depth Camera 없이 위치 계산

본 시스템은 Depth Camera를 이용하여 물체와 카메라 사이의 거리를 직접 측정하지 않는다.

대신 작업판을 평면으로 설정하고, 작업판 네 모서리에 배치한 ArUco Marker를 이용하여 카메라 영상 좌표를 작업판 좌표로 변환한다.

이를 통해 RGB 카메라만으로 작업판 위 폐기물의 XY 위치를 계산한다.

---

## 29.2 객체 인식과 위치 인식의 분리

YOLOv8은 폐기물의 종류와 Bounding Box를 판단하고, ArUco와 Homography는 해당 객체가 작업판의 어느 위치에 있는지를 계산한다.

즉, 각각의 기술을 다음과 같이 분리하여 사용한다.

```text
YOLOv8
  ↓
"무엇인가?"
  ↓
폐기물 종류 및 Bounding Box

ArUco + Homography
  ↓
"어디에 있는가?"
  ↓
작업판 XY 좌표
```

이러한 구조를 통해 객체의 종류를 판단하는 AI와 로봇의 위치 계산 기능을 분리하여 시스템을 구성한다.

---

## 29.3 비전 시스템과 로봇 시스템의 통합

본 시스템은 단순한 객체 탐지 시스템이 아니라 객체 인식 결과를 실제 로봇 동작으로 연결한다.

```text
객체 인식
   ↓
위치 계산
   ↓
좌표 변환
   ↓
ROS 2
   ↓
로봇 이동
   ↓
Pick
   ↓
Place
```

따라서 AI 기반 영상 인식과 로봇 자동화 기술을 하나의 시스템으로 통합한다.

---

# 30. 최종 시스템 구성도

```text
                         USB RGB Camera
                                │
                                ↓
                         OpenCV 영상 처리
                                │
                 ┌──────────────┴──────────────┐
                 │                             │
                 ↓                             ↓
           ArUco Marker                    YOLOv8
              검출                       Object Detection
                 │                             │
                 ↓                             ↓
       4개 모서리 기준점                   폐기물 검출
                 │                             │
                 ↓                             ↓
        작업판 XY 좌표계                 Bounding Box
             구성                              │
                                               ↓
                                      Bottom Center Point
                                               │
                 └──────────────┬──────────────┘
                                ↓
                       Homography 변환
                                ↓
                         작업판 X, Y 좌표
                                ↓
                        Robot Pick 좌표
                                ↓
                              ROS 2
                                ↓
                       OpenManipulator-X
                                ↓
                           Gripper Pick
                                ↓
                       분류 위치로 이동
                                ↓
                         Gripper Place
```

---

# 31. 결론

본 프로젝트에서는 RGB 카메라와 YOLOv8 기반 객체 인식 기술, ArUco Marker 기반 좌표계 구성, Homography 좌표 변환 기술 및 Robotis OpenManipulator-X를 결합하여 폐기물 자동 분류 시스템을 구성한다.

RGB 카메라를 이용하여 작업판 위의 폐기물을 촬영하고 YOLOv8을 이용하여 폐기물의 종류를 탐지한다.

동시에 작업판의 네 모서리에 배치된 4개의 ArUco Marker를 검출하여 작업판의 XY 좌표계를 구성한다.

YOLOv8이 생성한 Bounding Box의 하단 중앙점을 Bottom Center Point로 설정하고, 해당 픽셀 좌표를 Homography를 이용하여 작업판의 XY 좌표로 변환한다.

변환된 좌표는 ROS 2를 통해 로봇 제어 시스템으로 전달되며, OpenManipulator-X는 전달받은 목표 위치를 바탕으로 폐기물 위치로 이동한다.

OpenManipulator-X는 계산된 Pick 위치로 이동하여 폐기물을 Gripper로 집은 후, 폐기물의 종류에 따라 지정된 분류 위치로 이동하여 폐기물을 배치한다.

본 프로젝트의 핵심적인 기술적 특징은 다음과 같다.

```text
YOLOv8
  ↓
폐기물 종류 및 위치 탐지
  ↓
Bottom Center Point
  ↓
ArUco Marker
  ↓
작업판 좌표계 구성
  ↓
Homography
  ↓
작업판 XY 좌표 계산
  ↓
ROS 2
  ↓
OpenManipulator-X
  ↓
자동 Pick & Place
```

이를 통해 객체 인식 AI와 로봇 매니퓰레이터를 연계하여 폐기물의 인식부터 위치 계산, 파지 및 분류까지 이어지는 자동화 시스템의 구조를 구성한다.
