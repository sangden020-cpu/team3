# HRD 프로젝트 계획서

2026년 8월 10일

## 주제: AI 기반 재활용품 상태 인식 및 로봇팔 자동 분류 시스템

## 개인 역할 분담 및 실행

| 담당 | 역할 | 실행 내용 |
| --- | --- | --- |
| 팀원 1 | PM / 아키텍처 | 전체 일정 관리, 요구사항 정리, 시스템 구조 설계, 발표 자료 정리 |
| 팀원 2 | Pico 2W / Collector | Raspberry Pi Pico 2W MicroPython firmware, MQTT topic 설계, simulator 및 MQTT collector 구현 |
| 팀원 3 | Backend / Worker | FastAPI 서버, SQLite 저장소, 경고 판단, heartbeat/API/log 기능 구현 |
| 팀원 4 | Frontend / Desktop / QA | React/Vite 대시보드, WebSocket 실시간 갱신, pywebview 실행, 테스트 시나리오 점검 |

## 과제 목표

- 카메라와 AI 객체 인식 모델을 활용하여 재활용품의 종류와 상태를 실시간으로 인식하는 시스템을 구현한다.
- 캔, 종이, 플라스틱 병을 대상으로 객체의 종류와 구겨짐 상태를 판별한다.
- 캔은 정상 상태와 찌그러진 상태를 구분하고, 플라스틱 병은 구겨진 상태와 구겨지지 않은 상태를 구분한다.
- 구겨진 종이를 별도의 객체로 인식하여 재활용품 상태에 따른 분류가 가능하도록 한다.
- Roboflow에서 제공되는 학습 모델을 활용하고, 모델을 로컬 환경에 저장하여 인터넷 통신 없이 실시간 추론이 가능하도록 구성한다.
- OpenCV를 이용하여 카메라 영상을 수신하고 AI 인식 결과를 Bounding Box와 Class, Confidence 값으로 실시간 표시한다.
- 여러 개의 AI 모델을 하나의 카메라 영상에 적용하여 캔, 종이, 플라스틱의 상태를 동시에 판별할 수 있도록 한다.
- ROS 2 기반으로 AI 인식 결과와 로봇팔 제어 기능을 연동한다.
- OpenManipulator-X와 MoveIt 2를 이용하여 인식된 재활용품을 집고 지정된 위치로 이동시키는 자동 분류 기능을 구현한다.
- 카메라 인식, AI 추론, 로봇팔 제어 기능을 단계적으로 통합하여 재활용품 자동 분류 시스템을 완성한다.

## 수행 계획서

- USB 카메라를 OpenCV와 V4L2를 이용하여 연결하고 640×480 영상 데이터를 실시간으로 수신한다.
- OpenCV 카메라 영상에서 AI 객체 인식이 가능하도록 영상 입력 및 출력 기능을 구현한다.
- Roboflow에서 공개된 사전 학습 모델을 이용하여 재활용품 상태 인식 기능을 구현한다.
- 캔 상태 인식 모델을 이용하여 정상 캔과 찌그러진 캔을 구분한다.
- 캔은 `Intact`, `Slightly-crushed`, `Moderately-crushed`, `Crushed` 상태를 인식하도록 구성한다.
- 구겨진 종이 인식 모델을 이용하여 `crumpled paper` 객체를 검출한다.
- 플라스틱 병 인식 모델을 이용하여 `plastic-bottle`과 `crushed-plastic-bottle` 상태를 구분한다.
- 각 AI 모델별 Confidence Threshold를 설정하여 낮은 신뢰도의 오검출 결과를 제거한다.
- Roboflow Serverless API를 이용한 초기 모델 테스트를 수행한 후 `get_model()` 기반 로컬 추론 방식으로 전환한다.
- 최초 실행 시 AI 모델 가중치를 로컬 환경에 저장하고 이후 카메라 영상을 외부 서버로 전송하지 않고 PC 내부에서 추론하도록 구성한다.
- 여러 AI 모델의 추론으로 인해 카메라 영상이 끊기는 현상을 줄이기 위해 카메라 출력과 AI 추론을 별도의 처리 흐름으로 구성한다.
- `ThreadPoolExecutor`를 이용하여 카메라 영상 출력과 AI 추론 작업을 분리한다.
- AI 인식 결과를 객체의 Class, Confidence, Bounding Box 정보로 변환하여 화면에 표시한다.
- 캔, 종이, 플라스틱 인식 결과를 하나의 프로그램에서 통합하여 처리한다.
- AI 인식 결과를 ROS 2 노드에서 사용할 수 있도록 객체 종류, 상태, 위치 정보를 Topic 또는 내부 데이터 형식으로 전달한다.
- 카메라에서 검출된 Bounding Box의 중심 좌표를 이용하여 로봇팔이 조작해야 할 대상 객체를 선정한다.
- 필요 시 카메라 좌표와 로봇 좌표계 사이의 좌표 변환을 적용하여 실제 로봇팔 이동 위치를 계산한다.
- OpenManipulator-X와 MoveIt 2를 이용하여 목표 위치까지 로봇팔을 이동시킨다.
- Gripper를 이용하여 검출된 재활용품을 집고 객체 종류 및 상태에 따라 서로 다른 분류 위치로 이동시킨다.
- 정상 캔, 찌그러진 캔, 구겨진 종이, 정상 플라스틱 병, 구겨진 플라스틱 병에 대해 지정된 분류 동작을 수행하도록 구성한다.
- Gazebo 및 RViz2 환경에서 로봇팔 동작을 먼저 검증한 뒤 실제 OpenManipulator-X에 적용한다.
- 최종적으로 카메라 인식부터 로봇팔 Pick & Place까지 자동으로 수행되는 전체 흐름을 검증한다.

## 시행 목적

본 프로젝트의 목적은 카메라와 AI 객체 인식 기술, ROS 2, 로봇팔 제어 기술을 결합하여 재활용품의 종류와 상태를 자동으로 판별하고 분류하는 시스템을 구축하는 것이다.

카메라를 통해 작업 공간에 놓인 재활용품을 실시간으로 촬영하고, 사전 학습된 AI 모델을 이용하여 각 객체의 종류와 상태를 판별한다.

캔의 경우 단순히 캔의 존재 여부만 확인하는 것이 아니라 정상 상태와 찌그러진 상태를 구분하며, 플라스틱 병 역시 구겨진 상태와 구겨지지 않은 상태를 구분한다. 또한 구겨진 종이를 별도의 객체로 인식하여 재활용품의 재질뿐만 아니라 외형 상태까지 판단할 수 있도록 한다.

AI 모델은 초기에는 Roboflow Serverless API를 이용하여 모델 성능을 검증하고, 이후 모델을 로컬 환경에 저장하여 실시간 영상 데이터를 외부 서버로 전송하지 않고 PC 내부에서 직접 추론하도록 구성한다. 이를 통해 네트워크 지연으로 인한 영상 끊김을 줄이고 로봇 제어에 적합한 구조를 구현한다.

인식된 객체의 종류, 상태, 위치 정보는 ROS 2 시스템과 연동하며, OpenManipulator-X 로봇팔은 해당 정보를 이용하여 목표 객체에 접근한다. 이후 Gripper를 이용해 객체를 집고 분류 결과에 따라 지정된 위치로 이동시키는 Pick & Place 동작을 수행한다.

이를 통해 단순한 YOLO 객체 검출 실습에서 끝나는 것이 아니라 카메라 영상 처리, AI 기반 객체 및 상태 판별, 로컬 AI 추론, ROS 2 통신, 좌표 변환, MoveIt 2 경로 계획, Gripper 제어를 하나의 시스템으로 통합하는 것을 목표로 한다.

또한 각 기능을 단계적으로 개발하여 카메라 테스트, 단일 AI 모델 테스트, 다중 AI 모델 통합, ROS 2 연동, 로봇팔 제어 순서로 검증함으로써 복잡한 로봇 시스템의 개발 및 디버깅 과정을 학습한다.

## 사용 환경 및 사용 기술

| 구분 | 내용 |
| --- | --- |
| 운영 환경 | Ubuntu / WSL2 |
| Robot Framework | ROS 2 Jazzy |
| 로봇팔 | ROBOTIS OpenManipulator-X |
| 로봇 제어 | MoveIt 2, ros2_control |
| Gripper | OpenManipulator-X Gripper |
| 카메라 | USB Camera |
| 영상 처리 | OpenCV, V4L2 |
| AI Framework | Roboflow Inference, Supervision |
| AI 실행 방식 | `get_model()` 기반 로컬 추론 |
| 캔 인식 | 정상 및 찌그러짐 상태 Object Detection |
| 종이 인식 | Crumpled Paper Detection |
| 플라스틱 인식 | Normal / Crushed Plastic Bottle Detection |
| 비동기 처리 | Python `ThreadPoolExecutor` |
| 프로그래밍 언어 | Python |
| ROS 2 통신 | Topic, TF2 |
| 로봇 좌표 처리 | TF2, Camera Coordinate → Robot Coordinate 변환 |
| 시뮬레이션 | Gazebo |
| 시각화 | RViz2, OpenCV Window |
| AI 개발 환경 | uv 기반 Python 가상환경 |
| 버전 관리 | Git, GitHub |

## AI 인식 대상

| 분류 대상 | AI 인식 결과 | 의미 |
| --- | --- | --- |
| 캔 | `Intact` | 구겨지지 않은 정상 캔 |
| 캔 | `Slightly-crushed` | 약하게 찌그러진 캔 |
| 캔 | `Moderately-crushed` | 중간 정도로 찌그러진 캔 |
| 캔 | `Crushed` | 심하게 찌그러진 캔 |
| 종이 | `crumpled paper` | 구겨진 종이 |
| 플라스틱 병 | `plastic-bottle` | 구겨지지 않은 플라스틱 병 |
| 플라스틱 병 | `crushed-plastic-bottle` | 구겨진 플라스틱 병 |

## 시스템 구성

전체 시스템의 데이터 흐름은 다음과 같이 구성한다.

```text
USB Camera
     ↓
OpenCV 영상 수신
     ↓
AI 객체 인식
     │
     ├── 캔 상태 모델
     │     ├── Intact
     │     ├── Slightly-crushed
     │     ├── Moderately-crushed
     │     └── Crushed
     │
     ├── 구겨진 종이 모델
     │     └── crumpled paper
     │
     └── 플라스틱 병 상태 모델
           ├── plastic-bottle
           └── crushed-plastic-bottle
     ↓
객체 종류 / 상태 / 위치 판단
     ↓
ROS 2
     ↓
좌표 변환 및 목표 위치 결정
     ↓
MoveIt 2
     ↓
OpenManipulator-X
     ↓
Gripper Pick
     ↓
분류 위치로 이동
     ↓
Gripper Place