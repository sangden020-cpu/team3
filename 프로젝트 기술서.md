# 5. AI 객체 인식 시스템

## 5.1 YOLOv8 기반 객체 인식

본 프로젝트에서는 객체의 종류와 상태를 인식하기 위해 **YOLOv8 기반의 객체 탐지 모델**을 사용한다.

각 객체의 특성과 상태 분류 기준이 서로 다르기 때문에 하나의 통합 모델을 사용하는 대신, **종이, 플라스틱 병, 캔을 각각 별도의 YOLO 모델로 구성​**하여 객체를 인식한다.

각 모델은 다음과 같이 구성한다.

| 객체 | YOLO 모델 ID | 인식 대상 |
|---|---|---|
| 구겨진 종이 | `crumpledpaper/1` | crumpled paper |
| 플라스틱 병 | `plastic-bottles-ip5yb-uziag-hg1ll/1` | plastic-bottle, crushed-plastic-bottle |
| 캔 | `can-a8pgu/2` | Intact, Slightly-crushed, Moderately-crushed, Crushed |

Python 프로그램에서는 각 모델의 ID를 다음과 같이 정의한다.

```python
PAPER_MODEL_ID = "crumpledpaper/1"
PET_MODEL_ID = "plastic-bottles-ip5yb-uziag-hg1ll/1"
CAN_MODEL_ID = "can-a8pgu/2"
```

각 YOLO 모델은 입력된 RGB 영상에서 해당 객체의 Bounding Box와 클래스 정보를 출력한다.

전체적인 객체 인식 과정은 다음과 같다.

```text
                    RealSense RGB 영상
                           ↓
                    ┌──────┴──────┐
                    ↓             ↓             ↓
              Paper Model     PET Model      Can Model
                    ↓             ↓             ↓
             crumpled paper  plastic-bottle    Intact
                             crushed-plastic    Slightly-crushed
                              -bottle            Moderately-crushed
                                                Crushed
                    └─────────────┼─────────────┘
                                  ↓
                       객체 종류 및 상태 판단
                                  ↓
                         Bounding Box 추출
                                  ↓
                         중심 좌표 계산
```

---

## 5.2 종이 객체 인식 모델

구겨진 종이를 인식하기 위해 다음 YOLOv8 모델을 사용한다.

```python
PAPER_MODEL_ID = "crumpledpaper/1"
```

해당 모델은 RGB 영상에서 **crumpled paper** 객체를 탐지하고 Bounding Box를 출력한다.

검출된 Bounding Box의 중심 좌표는 이후 RealSense Depth 데이터와 결합하여 종이 객체의 3차원 위치를 계산하는 데 사용한다.

---

## 5.3 플라스틱 병 객체 인식 모델

플라스틱 병을 인식하기 위해 다음 YOLOv8 모델을 사용한다.

```python
PET_MODEL_ID = "plastic-bottles-ip5yb-uziag-hg1ll/1"
```

해당 모델은 플라스틱 병의 종류와 상태를 구분하여 인식한다.

- `plastic-bottle`
- `crushed-plastic-bottle`

탐지된 객체의 Bounding Box 중심 좌표와 Depth 데이터를 이용하여 해당 객체의 3차원 위치를 계산한다.

---

## 5.4 캔 객체 및 상태 인식 모델

캔의 상태를 구분하기 위해 다음 YOLOv8 모델을 사용한다.

```python
CAN_MODEL_ID = "can-a8pgu/2"
```

해당 모델은 캔의 형태 및 압착 상태에 따라 다음 네 가지 클래스로 구분한다.

- `Intact`
- `Slightly-crushed`
- `Moderately-crushed`
- `Crushed`

각 상태별 객체의 Bounding Box와 클래스 정보를 출력하며, 검출된 객체의 위치 정보는 이후 로봇의 Pick 위치를 결정하는 데 사용한다.

---

## 5.5 다중 YOLO 모델을 이용한 객체 인식 구조

본 시스템에서는 세 종류의 객체를 각각 독립적인 YOLO 모델로 처리한다.

따라서 하나의 영상 프레임이 입력되면 다음과 같은 방식으로 각각의 모델에서 객체를 탐지한다.

```text
                    RGB Frame
                        │
          ┌─────────────┼─────────────┐
          ↓             ↓             ↓
     Paper Model     PET Model     Can Model
          ↓             ↓             ↓
     Paper 결과      PET 결과       Can 결과
          └─────────────┼─────────────┘
                        ↓
              객체 종류 / 상태 결정
                        ↓
                 Bounding Box
                        ↓
                 중심 좌표 (u,v)
                        ↓
              RealSense Depth 정보
                        ↓
                  3D 좌표 (X,Y,Z)
```

이러한 구조를 통해 각 객체별로 특화된 학습 모델을 사용할 수 있으며, 객체의 종류뿐만 아니라 상태까지 구분할 수 있도록 구성한다.